ARLO SEO WORKLOG — append-only, NEWEST ON TOP.
Format per entry:  PAGE / CHANGE / TARGET / WHY / VERIFY.  End each session with NEXT:.
Read 12-week-strategy.md + README.md + audits/* before picking up NEXT.
Hard rules: build-gate every deploy; stage only files you changed; never 301 sister domains
(choquer.agency/choquer.app/futurlabs.dev); keep ARLO a standalone entity (no FuturLabs/Choquer copy).

=====================================================================================

## 2026-09-01 (Tue) — Monthly deep-dive: Month 2 review (August 2026) — SCORED Missed (0 of 4 measurable KPIs met)

SCORE — Month 2 goal: **Missed** (0 of 4 measurable KPIs from the 2026-08-01 goal list met). Content
production stayed strong for a 2nd straight month: 6 net-new indexable pages (Google Business Profile
connector + 5 blog posts — how-to-set-up-a-claude-mcp-server, client reporting, agency churn, MCP
server security, Google Ads reporting) plus 3 correctness/depth sessions on already-live pages
(OG/Twitter/schema fix across all 15 destination pages, comparison-table expansion + `dateModified`
across all 15, an on-page keyword retarget on the Search Console connector page) — 9 build-gated
sessions, zero regressions, matching or beating the ~2-pages/week quality bar every session this
month. But every metric outside the routine's direct control came up empty: live SE Ranking pull this
session (`PROJECT_getSummary` + `PROJECT_getPositionHistory`, 2026-08-01→2026-09-01, site_id
12704162) shows avg position flat at 100 (not-ranked floor) on **every single day of the month** —
0 of 22 tracked keywords measured, let alone in the top 50, against a goal of ≥15 measured / ≥3 in
top-50. `DATA_getBacklinksSummary` (domain mode): still 2 backlinks / 1 referring domain, identical to
the 2026-08-01 figure — off-page Phase 1 stayed at 0 of 6 all month, now 2 full months overdue (Week 4
target). `DATA_getAiSearchOverview` (US): still null brand_presence, 0 link_presence, 0
ai_opportunity_traffic — unchanged from every prior pulse. All 3 human-decision blockers set as Month 2
goals (catalog.ts/Meta Ads status, the dormant persona-route scaffold, GSC submission) are still open
at month's end; re-checked `catalog.ts` this session — `mailchimp`'s "August 2026" ETA has now also
lapsed (still `coming_soon`), meaning as of today all 5 gated connectors (meta_ads 4+ months overdue,
shopify 5+ months overdue, hubspot/mailerlite/mailchimp all past due) are simultaneously overdue for
the first time. One small positive: SE Ranking's own `domain_trust` for askarlo.app ticked 4→5, and
`index_google: "1"` confirms Google is indexing the domain despite GSC sitemap submission never having
been completed by a human.

Full monthly report (leads/CPL proxy, what's working/not, numbers-vs-Month-2-targets table, Month 3
goal) — see `reports/outbox/2026-09-01-monthly.json`. GA4 (`G-CP22NBPLFP`) still firing since 07-04 but
conversions remain unqueryable from this sandbox (no outbound GA4 API access) — leads/CPL stays
"pending" per the strategy doc's own honest frame; unlike Month 1, this month found no equivalent
self-serve fix for either off-page listings or GSC submission — both genuinely require a human with a
browser, which this sandbox does not have.

GOAL — Month 3 (September 2026), set this session:
1. First measured SERP position for **any** of the 22 tracked keywords (de-escalated from Month 2's
   ≥15 — a domain with zero ranked keywords after 2 full months needs to clear this lower bar before a
   top-50 target means anything).
2. Off-page Phase 1 shipped — even 2–3 of the 6 directory/registry listings (Smithery.ai, Glama.ai,
   mcp.so, awesome-mcp-servers GitHub PR, AlternativeTo, SaaSHub) would be the first referring-domain
   movement since kickoff; this is now the single highest-leverage open item on the whole plan and
   needs a human with a browser, not another session flagging it.
3. Human decisions closed: `catalog.ts` (now 5 of 5 gated connectors overdue — meta_ads, shopify,
   hubspot, mailerlite, mailchimp), the dormant 6-persona `route.ts` scaffold (open since 2026-07-30),
   and GSC sitemap submission actually completed (open since kickoff, 2026-07-04 — now 2 months).
4. Continue quality content pace into 12-week-strategy.md's Week 9–11 items now that the connector
   matrix and comparison hub are both fully caught up: long-tail connector variants (the
   ChatGPT-assistant-column variant, pending the still-open 2026-08-06 human check) and a `claude mcp
   server` pillar page (810/mo+ and climbing — the single highest-volume term in the whole thesis,
   never given its own dedicated page).
5. Leads/CPL proxy stays "pending" through Month 3 unless rankings actually appear; if they do, start
   pulling GA4 conversions as the real goal metric per the strategy doc's own W12 instruction.
6. Note: September 2026 lands at the end of the original 12-week window (kickoff 2026-07-04 + 12 weeks
   ≈ 2026-09-26) — the next monthly report should do the full W12 review: score months 1–3 together,
   refresh the keyword map with a live SE Ranking pull, and decide whether the goal metric flips toward
   leads or the ramp genuinely needs more runway given zero rankings through month 2.

## 2026-08-31 (Mon) — On-page retarget: `/connect/search-console-mcp` now leads with "Google Search Console MCP" — the exact-match phrase went from 0 to 390/mo (KD 16, competition 0.05) over the last 4 months while the page still only targeted "Search Console MCP" (50/mo)

CONTEXT: `sxo.md` quick-win #6 closed last session (2026-08-27) for its 3 named pages; connector matrix
(5/5 live Google sources) and comparison hub (5/5 named competitors) both still fully caught up;
`catalog.ts` re-checked again — `mailchimp` eta "August 2026" is now the current month with no ETA
slack left and still `coming_soon` (unchanged for a 6th session), `meta_ads`/`hubspot`/`mailerlite`/
`shopify`/`stripe` all still overdue, no new connector-matrix page available. Rather than open an 11th
blog post or re-scope the fresh-keyword-pull item cold, re-pulled live SE Ranking metrics (US) on the
FOCUS cluster's core terms first — `mcp for marketing`, `meta ads reporting`, `search console mcp`,
`google search console mcp`, `ga4 mcp`, `meta ads mcp`, `shopify mcp`, `hubspot mcp`, `conversational
analytics`, `supermetrics alternative`, `agencyanalytics alternative`, `whatagraph alternative` — to
check for drift against the 2026-07-04 keyword-research.md audit before picking a target. Found one
term with real, verified movement: `google search console mcp` now shows 390/mo (US), KD 16, competition
0.05, on a clean 0→170→215→260→390 climb over the last 4 months (`google-search-console-mcp` wasn't in
the original audit at all — an emerged term, not a stale re-pull). The live `/connect/search-console-mcp`
page (shipped 2026-07-06) only targeted the plain "Search Console MCP" phrasing — a live pull on that
exact term this session shows just 50/mo, an 8x smaller opportunity than the phrase actually gaining
share. Checked `agencyanalytics alternative` too (480/mo, KD 9 — the single highest-volume/lowest-KD
combination pulled this session) but it's a false lead: `comparisons.ts`'s `agencyanalytics-vs-arlo`
entry already leads its `metaTitle` with "AgencyAnalytics Alternative" (shipped, not a gap). `meta ads
mcp` (590/mo, up from 210) and `hubspot mcp` (20/mo) are both real demand but gated — Meta Ads and
HubSpot are still `coming_soon` in `catalog.ts`, so a dedicated connector page for either would
overclaim a connector that doesn't exist (same trap flagged repeatedly since 2026-07-20). `conversational
analytics` (320/mo, KD 38) remains P2 pillar-tier, not a quick win. This left the Search Console MCP
retarget as the single verified, human-independent, on-page (not new-page) opportunity — smaller in
scope than a new page, but "quality over volume" per the routine's own hard rules, and the page already
exists/is already indexed, so this is a pure upside capture with no cannibalization or new-content risk.
- PAGE: `/connect/search-console-mcp` (existing, retargeted — no new page, no slug change, no internal
  links broken) / CHANGE: `src/content/connectors.ts`, the `search-console-mcp` entry only. `title`:
  "Search Console MCP: Connect GSC to Claude" → "**Google Search Console MCP**: Connect GSC to Claude"
  (renders as the page's literal `<h1>` and the `WebPage` JSON-LD `name`). `metaTitle`: "Search Console
  MCP | Connect Google Search Console to Claude — ARLO" → "**Google Search Console MCP** | Connect GSC
  to Claude — ARLO" (the `<title>` tag — now leads with the exact 390/mo phrase instead of burying it
  mid-string). `metaDescription` and `tldr` both rewritten to open with "ARLO's Google Search Console
  MCP" instead of "Connect Google Search Console..." — same facts, phrase moved to the front where it
  reads most naturally and carries the most on-page weight. `why.heading` (an `<h2>`): "Why connect
  Search Console to Claude..." → "Why a **Google Search Console MCP** beats digging through the GSC
  UI" — one more on-page keyword surface, still reads naturally as a section header. `why.body`: one
  clause changed ("An MCP connector skips the UI" → "A **Google Search Console MCP** connector skips
  the UI entirely") — not a full rewrite, the rest of the section's substance (weekly re-run pain, CSV
  comparison) is unchanged. One FAQ question retargeted: "Is this Google's official Search Console MCP
  server?" → "Is ARLO's **Google Search Console MCP** an official Google product?" (answer edited to
  match, same "no, third-party, OAuth to your own properties" substance — not a new claim). `lastUpdated`
  bumped 2026-07-06 → 2026-08-31 (matches the site's own `dateModified`-freshness convention, established
  2026-08-27). Also updated `public/llms.txt`'s one-line description of this page ("Search Console MCP"
  → "Google Search Console MCP") so the AI-crawler-facing page list matches the page's own new title —
  no other file references the page's title/metaTitle strings (checked via grep: internal links,
  `relatedConnectorSlugs`, and blog cross-links all reference the unchanged slug `search-console-mcp`
  or generic anchor text "Search Console", neither of which needed touching). / TARGET: primary **`google
  search console mcp`** — 390/mo (US), KD 16, competition 0.05, live SE Ranking pull this session,
  rising every month for 4 consecutive months (0→170→215→260→390) — genuinely emerged demand, not a
  stale audit figure. Secondary, unchanged and still served: `search console mcp` (50/mo, KD 18) and
  `connect google search console to claude` (the page's existing how-to framing). / WHY: verified via a
  fresh live pull (not the 2026-07-04 static audit, which didn't carry this exact phrasing) that the
  page's current title/meta target a phrase now carrying roughly 1/8th the volume of a near-identical,
  near-zero-competition variant it doesn't mention anywhere in its `<title>` or `<h1>` — a precise,
  low-risk on-page fix rather than a new content bet, matching the routine's "quality over volume"
  instruction better than a marginal 11th blog post or a new connector page gated on an unshipped
  catalog entry. / VERIFY: `rm -rf .next && npm install && npm run build` — `✓ Compiled successfully`,
  exit 1 only from the same 9 pre-existing `(app)/*` + `/welcome` prerender failures as every prior
  session (missing `NEXT_PUBLIC_CONVEX_URL`, Vercel-only secret) — identical route list, not a
  regression. `npm run lint` clean (only the same pre-existing unrelated `sitemap/page.tsx` CSS-tag
  warning). Grepped the built static HTML directly
  (`.next/server/app/connect/search-console-mcp.html`): confirmed `<title>Google Search Console MCP |
  Connect GSC to Claude — ARLO</title>`, `"name":"Google Search Console MCP: Connect GSC to Claude"` in
  the `WebPage` JSON-LD, and the phrase "Google Search Console MCP" appearing 31 times total across the
  rendered page (H1, TL;DR, why-section heading/body, FAQ question/answer) — not a single isolated
  mention. `git status` showed only `src/content/connectors.ts` + `public/llms.txt` after reverting the
  build-regenerated `src/content/blog/manifest.json` diff (same pattern as every prior session) — staged
  only those 2 intended files.

NEXT:
1. `mailchimp` eta "August 2026" is now the **current month with no days of ETA slack left** — 6th
   consecutive session unchanged, still `coming_soon`. Worth a human confirming ship status this week;
   if it flips to `live`, that's the first new connector-matrix page available in 5+ months
   (`/connect/mailchimp-mcp`). Everything else in catalog.ts unchanged and overdue: `meta_ads` (4+
   months, and this session confirmed real live demand for `meta ads mcp` at 590/mo — a page is ready
   to write the moment the connector ships), `hubspot`/`mailerlite` (past due), `shopify` (5+ months,
   most overdue entry), `stripe` (past due).
2. `sxo.md` quick-win #6 remains closed (no change this session — not touched). The 12 unreframed
   `/destinations/[slug]` pages are still thin but weren't named by the audit's own priority list.
3. Human decisions still open (unchanged, now 14+ sessions): the dormant 6-persona `route.ts` scaffold
   (open since 2026-07-30); GSC submission (open since kickoff, 2026-07-04). Off-page Phase 1 still 0
   of 6, most overdue item on the whole plan (flagged since Week 4) — needs a human with a browser,
   this sandbox has no outbound HTTP.
4. The ChatGPT-assistant-column connector-page variant flagged 2026-08-06 is still an open human-check
   item (confirm ChatGPT connector support is actually tested/live before building pages for it).
5. This session's live SE Ranking pull cleared `agencyanalytics alternative` (480/mo, KD 9) as
   already-shipped (not a gap) and confirmed `conversational analytics` (320/mo, KD 38) remains P2
   pillar-tier, not a quick win — a future content session doesn't need to re-check either. The one
   still-open, still-gated opportunity surfaced this session is `meta ads mcp` (590/mo, up from 210) —
   do not build a page for it before `meta_ads` in `catalog.ts` flips to `live` or `beta`.
6. Next weekly-pulse-style session (backlinks, CI status, catalog.ts re-check, full rank-position
   report) due — last full pulse was 2026-08-28, established Friday cadence puts the next one around
   2026-09-04.

## 2026-08-28 (Fri) — Weekly pulse: rank positions still flat 0/22 after 4th full week; Month 2 closed with neither KPI met; catalog.ts/off-page blockers unchanged. See reports/outbox/2026-08-28-weekly-pulse.json.

## 2026-08-27 (Thu) — Added "alternative to X" comparison sections (feature table + when-to-choose lists) to the 3 highest-searched `/destinations/[slug]` pages (Looker Studio, Power BI, BigQuery); added missing `dateModified` to all 15 destination pages' WebPage schema

CONTEXT: session started by discovering the 2026-08-24 session's two commits
(`99835a7` schema/metadata fix, `0f516ca` archive) existed only in a detached
HEAD in this sandbox's clone, never reachable from `origin/main` — confirmed
via `git merge-base --is-ancestor` before touching anything, fast-forwarded
local `main` to that commit, then re-fetched and found `origin/main` was
already at the same commit (a stale local remote-tracking ref from clone
time, not an actual unpushed-work incident) — no data was at risk, but
worth flagging in case it recurs. `catalog.ts` re-checked and still
unchanged this session: `mailchimp` (Aug 2026 eta, now past due),
`meta_ads`/`hubspot`/`mailerlite`/`shopify`/`stripe` all still overdue — no
new connector-matrix page available. With the connector matrix (5/5 live)
and comparison hub (5/5 named competitors) both still fully caught up, and
the 2026-08-24 correctness fix already closed, moved to the top of NEXT
item 1: `sxo.md` quick-win #6 / Section 8 item 6, open since the 2026-07-04
audit — reframe the 3 most-searched "alternative to X" `/destinations/[slug]`
pages with comparison depth.
- PAGE: `/destinations/looker_studio`, `/destinations/power_bi`,
  `/destinations/bigquery` (comparison section, new) + all 15
  `/destinations/[slug]` pages (`dateModified` schema field, new) / CHANGE:
  two files. (1) `src/app/destinations/[slug]/route.ts` — added
  `COMPARISON_CONTENT`, a record keyed by destination id holding a TL;DR
  intro, a `[toolName]` vs. ARLO feature table (6 rows for Looker
  Studio/Power BI, 5 for BigQuery: setup, cost/licensing, data
  connections/freshness, scale, skill required), and two when-to-choose
  lists ("Skip `[tool]` when" / "Keep `[tool]` when") — only populated for
  `looker_studio`, `power_bi`, `bigquery`; every other id resolves to an
  empty string via `comparisonSectionHtml()`, same optional-section pattern
  used for `faqSchemaHtml` in the prior session (the shared
  `_template/_shell.html` mustache-lite has no `{{#if}}`, so an
  empty-string content var is how a section stays page-specific). Table/
  when-grid renderer functions and their inline styles are copied from the
  proven `/compare/[slug]/route.ts` pattern (`comparisonTable`,
  `whenGrid`) for visual consistency with the site's other comparison
  content, not reinvented. Also added a shared `CONTENT_LAST_UPDATED`
  constant, passed into `content.dateModified` for every destination page.
  (2) `src/app/destinations/_template/_shell.html` — added `"dateModified":
  "{{dateModified}}"` to the existing `WebPage` JSON-LD block, and a new
  `{{comparisonHtml}}` placeholder between the hero block and the "How it
  works" section (empty on the other 12 pages). / TARGET: no new keyword —
  this closes the SXO intent-mismatch flagged in `audits/sxo.md` (Section
  3's SERP-consensus finding that "[BI tool] alternative" queries reward
  Comparison-type content; Section 8 quick-win #6 named these exact 3
  pages: Looker Studio, Power BI, BigQuery). Existing/expected organic
  terms these pages already partially rank for or could: "looker studio
  alternative", "power bi alternative", "bigquery alternative for
  marketing reporting" — commercial-intent, low-competition per the
  audit's Section 3 SERP read (personal blogs/micro-tools, not DA-88
  fortresses). / WHY: these 3 pages serve two distinct search intents —
  "I already use `[tool]`, connect ARLO's live data to it" (the page's
  existing content) and "I'm evaluating whether I need `[tool]` at all"
  (previously unserved — audit Section 3's Budget-Conscious Solo Business
  Owner persona explicitly names this gap: "wants to know if a no-ETL,
  no-warehouse tool exists... blocked by price/format sensitivity and
  unclear differentiation... closest matching content is a thin feature
  page with no pricing comparison or 'why leave Looker Studio' framing").
  Framed the new section honestly as serving both readers ("ARLO is
  both") rather than pretending the destination-connector page is purely a
  competitor teardown — it still primarily explains how to push ARLO data
  into these tools. No competitor pricing figures were invented: Looker
  Studio's "free" status is stated plainly (uncontroversial, stable fact);
  Power BI and BigQuery are described only in directional/structural terms
  (per-user licensing required to share, usage-based storage+query
  pricing) rather than specific dollar amounts this sandbox has no way to
  verify live (no outbound web access) — same caution the 2026-08-20 blog
  post applied to invented statistics. Bundled the `dateModified` fix
  (sxo.md Section 8 item 8, a one-line addition to the same `WebPage`
  schema block already being touched for a real reason) rather than
  opening a second session for it. / VERIFY: `rm -rf .next && npm install
  && npm run build` — `✓ Compiled successfully`, exit 1 only from the same
  9 pre-existing `(app)/*` + `/welcome` prerender failures as every prior
  session (missing `NEXT_PUBLIC_CONVEX_URL`, Vercel-only secret) — same
  route list, not a regression. `npm run lint` clean (only the same
  pre-existing unrelated `sitemap/page.tsx` CSS-tag warning). Verified via
  `next dev` on a scratch port (this route has no static `.html` output,
  same known Convex-env gap as the 2026-08-24 session): confirmed the new
  comparison section (feature table + both when-lists) renders on
  `/destinations/looker_studio`, `/destinations/power_bi`, and
  `/destinations/bigquery`, is absent from a control page
  (`/destinations/tableau`), and `dateModified: "2026-08-27"` appears in
  the `WebPage` JSON-LD on all 4 spot-checked pages. `git status` showed
  only the 2 intended files after reverting the build-regenerated
  `src/content/blog/manifest.json` diff (same pattern as every prior
  session) — staged only those 2 files.

NEXT:
1. **`sxo.md` quick-win #6 is now closed** for the 3 named pages. The
   remaining 12 `/destinations/[slug]` pages are still thin
   (~150–250 words, shared prompts/FAQ only) but weren't named by the
   audit's own priority list — leave as-is unless a future keyword pull
   surfaces demand for one of them specifically (e.g. "snowflake
   alternative", "tableau alternative").
2. **`mailchimp` eta "August 2026" is now the current month and, per this
   session's date, effectively due** — still `coming_soon` in catalog.ts,
   unchanged for the 5th consecutive session. Worth a human confirming
   ship status; if it flips to `live`, that's the first new
   connector-matrix page available in 5+ months (`/connect/mailchimp-mcp`).
   Everything else in catalog.ts unchanged and overdue: `meta_ads` (3+
   months), `hubspot`/`mailerlite` (past due), `shopify` (4+ months, most
   overdue entry), `stripe` (past due).
3. Human decisions still open (unchanged, now 13+ sessions): the dormant
   6-persona `route.ts` scaffold (open since 2026-07-30); GSC submission
   (open since kickoff, 2026-07-04). Off-page Phase 1 still 0 of 6, most
   overdue item on the whole plan (flagged since Week 4) — needs a human
   with a browser, this sandbox has no outbound HTTP.
4. The ChatGPT-assistant-column connector-page variant flagged 2026-08-06
   is still an open human-check item (confirm ChatGPT connector support is
   actually tested/live before building pages for it).
5. A fresh-keyword-pull blog post (the pattern used 2026-08-20) remains a
   viable human-independent lever if a future session prefers content over
   more on-page/technical fixes — re-pull rather than assume
   `keyword-research.md`'s static list is current.
6. Next weekly-pulse-style session (backlinks, CI status, catalog.ts
   re-check, full rank-position report) due — last full pulse was
   2026-08-21, established Friday cadence puts the next one around
   2026-08-28, right after this session.
7. **Verify at session start that `origin/main` actually matches what
   `git log` shows** (`git fetch origin main` before trusting a cached
   `origin/main` ref) — this session's local clone had a stale
   remote-tracking ref that briefly looked like unpushed prior-session
   work; harmless this time, but worth a fast check before relying on it.

## 2026-08-24 (Mon) — Fixed broken OG/Twitter meta + wrong-page WebPage schema + missing FAQPage schema across all 15 `/destinations/[slug]` pages

CONTEXT: catalog.ts unchanged again this session (mailchimp still `coming_soon` eta "August 2026" —
now current-month and unshipped with a week left; meta_ads/hubspot/mailerlite/shopify/stripe all still
past their own ETAs) — no new connector to build. Connector matrix (5/5 live) and comparison hub (5/5
named competitors) both remain fully caught up. Rather than ship an 11th blog post in a row (last 5
sessions were all content), re-read `audits/sxo.md` (2026-07-04) for still-open, human-independent P1
items: quick-win #6 flags `/destinations/[slug]` as the second-weakest persona page type (31/100,
"Integration Evaluator") and recommends reframing 2–3 pages with comparison depth. While scoping that,
found something more clearly broken and higher-leverage first: the shared detail-page shell
(`src/app/destinations/_template/_shell.html`, used by all 15 `/destinations/[slug]` routes) hardcoded
`og:title`/`og:description`/`twitter:title`/`twitter:description` to a generic, wrong-page string
("ARLO for SEO Specialists — Ask Claude About Any Client" + Search-Console/GA4/PageSpeed/GBP copy) on
*every* destination page — so sharing `/destinations/looker_studio` or `/destinations/power_bi` on
Slack/social/any OG-scraper showed the SEO-specialist persona page's title, not the destination's own.
Same root-cause bug in the page's `WebPage` JSON-LD: `name`/`url`/`description` were all hardcoded to
the homepage (`url: "https://askarlo.app"` on all 15 pages — the exact class of bug P0 #3's site-wide
canonical fix addressed elsewhere, missed here because this page type renders through a separate
hand-rolled HTML shell, not the shared Next.js metadata system). The breadcrumb JSON-LD was also
2-level ("Home / ARLO", both pointing at the homepage) instead of reflecting the real
Destinations-hub → page hierarchy. Separately: despite every one of the 15 pages rendering a live FAQ
block (5 shared Q&As), none had `FAQPage` schema — confirmed via grep, only 2 `ld+json` blocks exist in
the whole shell (Organization/WebSite/SoftwareApplication, and WebPage/BreadcrumbList), no FAQPage. The
sibling hub page `/destinations` does have hand-maintained FAQPage schema (per its own file comment,
"FAQ items MUST match the FAQPage JSON-LD in _shell.html") — the 15 detail pages never got the same
treatment. Fixed all of it in one pass rather than shipping the comparison-table content expansion
first, since a wrong/missing schema and mismatched social previews on 15 already-indexed pages is a
correctness bug, not a depth gap — higher priority per the P0-style precedent already set on this site.
- PAGE: all 15 `/destinations/[slug]` pages (looker_studio, power_bi, tableau, google_sheets, excel,
  bigquery, snowflake, redshift, databricks, slack_digest, notion_destination, airtable_destination,
  shareable_dashboard, email_digest, pdf_report) / CHANGE: two files.
  (1) `src/app/destinations/_template/_shell.html` — `og:title`/`twitter:title` → `{{title}}`,
  `og:description`/`twitter:description` → `{{metaDescription}}` (both already-available, already-used
  content vars — same value already correctly powering `<title>` and the meta-description tag, just
  never wired to the social tags). `WebPage` JSON-LD `name`/`url`/`description` → `{{title}}` /
  `{{canonicalUrl}}` / `{{metaDescription}}`. Breadcrumb JSON-LD expanded to 3 levels: Home →
  Destinations (`https://askarlo.app/destinations`) → the page's own name/canonical URL. Added a
  `{{faqSchemaHtml}}` placeholder immediately after the existing schema scripts.
  (2) `src/app/destinations/[slug]/route.ts` — added a `faqSchemaHtml()` helper that builds `FAQPage`
  JSON-LD directly from the same `FAQ.items` array already used to render the visible FAQ block (not a
  hand-duplicated copy — the two can't drift the way the hub page's manual pair could), passed into
  `content.faqSchemaHtml`. JSON is `JSON.stringify`'d (safe escaping) with `<` replaced by `<` as a
  defensive guard against premature `</script>` termination. / TARGET: no new keyword — this is a
  correctness/schema-parity fix on already-indexed pages targeting the existing `/destinations/*`
  organic + AI-citation surface (each page's own tool-specific terms, e.g. "looker studio", "power bi",
  "bigquery" + "arlo"/"claude" context) and, more directly, GEO/AI-crawler accuracy (`llms.txt`,
  AI-Overview citation, and any LLM parsing OG/JSON-LD to describe what a specific destination page is
  about) and human link-preview accuracy when these pages are shared. / WHY: `audits/sxo.md` (quick-win
  #6, Section 8 item 6) flagged `/destinations/[slug]` as thin/mismatched-intent; while reading the
  template to plan a comparison-table content expansion, found the more urgent, purely-mechanical bug
  above — wrong metadata shipping on 15 live pages is a higher-confidence, lower-risk, more immediately
  correctable issue than a content redesign, and matches the audit's own repeated finding pattern (P0 #3
  canonical bug, P1 #8 FAQPage-on-FAQ-component) of "the data model is fine, the wiring to this
  particular page type was missed." The comparison-table/feature-depth reframe recommended by
  `sxo.md` quick-win #6 remains open for a future session — flagged in NEXT below, scoped as a design/
  content task distinct from this correctness fix. / VERIFY: `rm -rf .next && npm install && npm run
  build` — compiled successfully (`✓ Compiled successfully`, exit 0); same 9 pre-existing `(app)/*` +
  `/welcome` prerender failures as every prior session (missing `NEXT_PUBLIC_CONVEX_URL`, Vercel-only
  secret) — confirmed identical route list, not a regression. `/destinations/[slug]` is a hand-rolled
  `route.ts` (no static `.html` output, same as the homepage's `arlo/route.ts`), so `next start` doesn't
  work in this sandbox (missing `prerender-manifest.json` from the same known Convex-env gap) — verified
  instead via `next dev` on a scratch port and `curl`+Python-parsed the live response for
  `/destinations/looker_studio`, `/destinations/power_bi`, `/destinations/bigquery`: confirmed per-page
  `og:title` (e.g. "Looker Studio — BI & Dashboards Destination | ARLO", "Power BI — BI & Dashboards
  Destination | ARLO" — no longer the SEO-specialist string), correct `canonical` per page, `WebPage`
  JSON-LD `name`/`url`/`description` matching the real page (not the homepage), 3-level breadcrumb with
  the correct leaf name/URL, and well-formed `FAQPage` JSON-LD present and matching the rendered FAQ
  items verbatim on all three spot-checked pages. `npm run lint` clean (only the same pre-existing
  unrelated `sitemap/page.tsx` CSS-tag warning). `git status` showed only the 2 intended files touched
  after reverting the build-regenerated `src/content/blog/manifest.json` diff (same pattern as every
  prior session) — staged only those 2 files.

NEXT:
1. **`sxo.md` quick-win #6 remains open**: reframe 2–3 top `/destinations/[slug]` pages (Looker Studio,
   Power BI, BigQuery — highest-searched "alternative to X" terms per Section 3) with explicit
   comparison framing, a feature table, and pricing context — currently ~150–250 word single-screen
   pages. This is a content/design task, distinct from this session's schema/metadata correctness fix;
   the shared `_template/_shell.html` has no `{{#if}}` support (`arlo/_render.ts` mustache-lite is
   `{{#each}}`/`{{var}}` only), so a per-page-optional section needs a pre-rendered HTML-string content
   var (same pattern used here for `faqSchemaHtml`), not a template conditional.
2. **`mailchimp` eta "August 2026" is now current-month with about a week left** — still `coming_soon`
   in catalog.ts, unchanged for the 4th consecutive session. Worth a human confirming ship status; if it
   flips to `live`, that's the first new connector-matrix page available in 5+ months
   (`/connect/mailchimp-mcp`). Everything else in catalog.ts unchanged and overdue: `meta_ads` (3+
   months), `hubspot`/`mailerlite` (past due), `shopify` (4+ months, most overdue entry), `stripe`
   (past due).
3. Human decisions still open (unchanged, now 12+ sessions): the dormant 6-persona `route.ts` scaffold
   (open since 2026-07-30); GSC submission (open since kickoff, 2026-07-04). Off-page Phase 1 still
   0 of 6, most overdue item on the whole plan (flagged since Week 4) — needs a human with a browser,
   this sandbox has no outbound HTTP.
4. The ChatGPT-assistant-column connector-page variant flagged 2026-08-06 is still an open human-check
   item (confirm ChatGPT connector support is actually tested/live before building pages for it).
5. A fresh-keyword-pull blog post (the pattern used 2026-08-20) remains a viable human-independent
   lever if a future session prefers content over more on-page/technical fixes — re-pull rather than
   assume `keyword-research.md`'s static list is current.
6. Next weekly-pulse-style session (backlinks, CI status, catalog.ts re-check, full rank-position
   report) due — last full pulse was 2026-08-21, established Friday cadence puts the next one around
   2026-08-28.

## 2026-08-21 (Fri) — Weekly SEO pulse: rank positions still flat 0/22 after a 3rd full week (Month 2 KPIs now off track as August closes); catalog.ts drift — every coming_soon entry now past its own ETA, mailchimp's Aug 2026 date is due and still unshipped, shopify (Apr 2026) is now the most-overdue entry in the catalog; CI 23/24 green, off-page still 0/6, GSC/GA4 still pending — see reports/outbox/2026-08-21-weekly-pulse.json.

## 2026-08-20 (Thu) — "Google Ads Reporting for Agencies" blog post — closes the highest-volume unaddressed P1 keyword left in keyword-research.md's Cluster 3

SHIPPED — `src/content/blog/posts/us/google-ads-reporting-for-agencies.md` (new). Acted on the
2026-08-17 NEXT item 3, which explicitly said the original content.md 12-post skeleton is exhausted of
non-redundant angles and that a future session should **re-scope from a fresh keyword pull rather than
the static skeleton before shipping post #10**. Did exactly that: pulled live SE Ranking metrics (US)
for the still-unshipped P1 terms in `keyword-research.md`'s Cluster 3/4, then ran
`DATA_getRelatedKeywords` off the winner to check the surrounding cluster before committing.
- PAGE: /blog/google-ads-reporting-for-agencies (new) / CHANGE: new markdown post via the existing
  `src/content/blog/posts/us/*.md` → `getAllBlogPosts()` → `/blog/[slug]` pipeline (BlogPosting JSON-LD
  auto-generated, no template changes). 1,332 words (in range with the 9 existing posts, 1,112–2,301). /
  TARGET: primary **`google ads reporting`** — pulled live this session: 390/mo, KD 30, CPC $12.66,
  competition 0.21. This is the **highest-volume P1 term in the whole audit that has never had a page**
  (keyword-research.md Cluster 3 lists it at 480/mo, KD 25, P1; the live re-pull shows 390/mo, KD 30 —
  reported the current live figure, did not reuse the stale audit number). Secondaries, all confirmed
  live and all lower-KD than the primary: `google ads reporting tool` (140/mo, KD 20),
  `google ads report template` (110/mo, KD 14), `advertising reporting` (170/mo, KD 14 — surfaced by
  `getRelatedKeywords`, not in the original audit). Also checked and rejected this session:
  `automated reporting tools` (210/mo KD 24) and `automated marketing reports` (50/mo KD 24) — both on
  a clear 12-month *decline* (390→170 and 90→50 respectively), so not worth a page ahead of a rising
  term; `conversational analytics` (320/mo, KD 38) — P2 tier and also declining since Feb;
  `meta ads reporting` (110/mo, KD 18) — real demand, but Meta Ads is still `coming_soon` in
  `catalog.ts`, so a post targeting it would advertise a connector that doesn't exist (same
  overclaiming trap the worklog has flagged since 2026-07-20). Google Ads is `status: "live"`, so this
  post only describes shipped capability. / WHY: the connector matrix (5/5 live Google sources) and
  comparison hub (5/5 named competitors) are both still fully caught up — re-checked `catalog.ts` again
  this session, unchanged: `meta_ads` eta "May 2026" (3+ months overdue), `hubspot` eta "July 2026" and
  `mailerlite` eta "July 2026" both past due, `mailchimp` eta "August 2026" now current-month and worth
  a human check next session. Content remains the only human-independent lever, and this closes a real
  volume gap rather than adding a 10th post for its own sake. / CONTENT: structured around the actual
  reporting job (5 metrics that belong in a client-facing Google Ads report, why MCC account-switching
  is the real time sink, a cadence section separating weekly-internal / monthly-client / quarterly-QBR,
  and a plain report-template structure) before getting to ARLO — the same "useful even if you never
  buy" pattern as the client-reporting and churn posts. Every ARLO claim reused only what's already
  live on `/connect/google-ads-mcp` (verified by reading `src/content/connectors.ts` first): MCC
  sub-account assignment, no GAQL required, pass-through/nothing warehoused, and the metric/dimension
  list (impressions, clicks, CTR, avg CPC, cost, conversions, conversion value at campaign + ad-group
  level). Carried over that page's own honest "not yet supported" FAQ verbatim in substance — Quality
  Score and search-term reports are explicitly called out as roadmap, not silently omitted. The
  ~2.5 hrs/week time-cost figure is labeled as illustrative math (15 clients × 10 min), same caution as
  the client-reporting post; no invented competitor pricing or external statistics (no outbound web
  access to verify any). Internal links: `/connect/google-ads-mcp`, `/connect/google-analytics-mcp`,
  `/connect/search-console-mcp`. No manual relatedPosts wiring needed (automatic via
  `getAllBlogPosts()`). Did not add to `public/llms.txt` — same precedent as all 9 prior posts.
- VERIFY: `rm -rf .next && npm install && npm run build` — compiled successfully; same 9 pre-existing
  `(app)/*` + `/welcome` prerender failures as every prior session (missing `NEXT_PUBLIC_CONVEX_URL`,
  Vercel-only secret) — confirmed identical route list, not a regression. `/blog/[slug]` has no
  `generateStaticParams` (dynamic route, no static `.html` to grep), so verified two ways: via `npx
  tsx` (`getAllBlogSlugs()` includes the new slug, `getAllBlogPosts()` sorts it first at date
  2026-08-20, `getBlogPostBySlug()` resolves title/tags/wordCount 1,332), and by grepping the built
  `.next/server/app/blog.body` — both the new slug and its rendered title appear in the live blog-hub
  output, confirming it's actually linked, not just parseable. `npm run lint` clean (only the same
  pre-existing unrelated `sitemap/page.tsx` CSS-tag warning). `git status` showed only
  `src/content/blog/manifest.json` (build-regenerated, reverted) + the new post — staged only the one
  intended file.

NEXT:
1. **`mailchimp` eta "August 2026" is current-month** — first `catalog.ts` ETA that isn't already
   overdue. Worth a human confirming whether it ships this month; if it flips to `live`, that's the
   first new connector-matrix page available in 4+ months (`/connect/mailchimp-mcp`). Everything else
   in catalog.ts unchanged and overdue: `meta_ads` (3+ months), `hubspot`/`mailerlite` (past due).
2. Human decisions still open (unchanged, now 11+ sessions): the dormant 6-persona `route.ts` scaffold
   (open since 2026-07-30); GSC submission (open since kickoff, 2026-07-04). Off-page Phase 1 still
   0 of 6, most overdue item on the whole plan (flagged since Week 4) — needs a human with a browser,
   this sandbox has no outbound HTTP.
3. The ChatGPT-assistant-column connector-page variant flagged 2026-08-06 is still an open human-check
   item (confirm ChatGPT connector support is actually tested/live before building pages for it).
4. The fresh-keyword-pull approach used this session found one more viable, still-unshipped angle worth
   a future post: `meta ads reporting` (110/mo, KD 18, stable) — but it is **gated on `meta_ads`
   flipping to `live` in catalog.ts**, so do not ship it before item 1 above resolves. Beyond that, the
   remaining P1 Cluster-3 terms are now all either covered by a live page or on a measurable decline;
   a future content session should re-pull rather than assume this list is still current.
5. A weekly-pulse-style session (backlinks, CI status, catalog.ts re-check, full rank-position report)
   is overdue — last full pulse was 2026-08-14; the established Friday cadence puts the next one at
   2026-08-21.

## 2026-08-17 (Mon) — "MCP Server Security" blog post — closes the standing content.md trust/security gap (item 12, flagged since kickoff)

SHIPPED — `src/content/blog/posts/us/mcp-server-security.md` (new). Re-read NEXT lists back to
2026-08-13 first: item 4 named two remaining open blog angles — a GA4-specific 3-minute-setup piece,
and "what actually happens to your clients' data when you connect an MCP tool" (content.md item 12,
a dedicated trust/security piece distinct from the passing security-FAQ mentions already live).
Picked the security piece over the GA4 setup piece: the GA4 angle would heavily duplicate the already-
live `/connect/google-analytics-mcp` page and the `how-to-set-up-a-claude-mcp-server` post, while the
security piece fills a genuine, still-open gap — checked `audits/content.md` §1.4/§21 confirms this is
the highest-weighted, still-unaddressed E-E-A-T factor (Trustworthiness, 30% weight) with no dedicated
page anywhere on the site. Also confirmed connector matrix (5/5 live) and comparison hub (5/5 named
competitors) are both still fully caught up, and `catalog.ts` still unchanged (meta_ads/hubspot/
mailerlite all past their own ETAs) — content remained the only human-independent lever.
- PAGE: /blog/mcp-server-security (new) / CHANGE: new markdown post via the existing
  `src/content/blog/posts/us/*.md` → `getAllBlogPosts()` → `/blog/[slug]` pipeline (BlogPosting
  JSON-LD auto-generated, no template changes). 1,727 words (in range with the 8 existing posts). /
  TARGET: primary **`mcp server security`** — checked live via `DATA_getKeywordsMetrics` (US) first:
  170/mo, KD 34, rising fast (40→170 over the last 12 months, steady climb every month) — real,
  growing demand, P2 tier per keyword-research.md's own priority key (KD 26–45). Secondary `mcp
  security risks` (50/mo, KD 38). Checked several more specific phrasings first (`is mcp safe`, `is
  claude mcp safe`, `mcp data security`, `client data security marketing agency`) — all
  `is_data_found: false`, so didn't force a page onto invented volume; led with the term that actually
  has measurable, rising demand instead. / WHY: closes the most-repeated remaining open content item
  (named in every NEXT list since content.md's original 2026-07-04 audit) and directly answers the
  question a compliance-conscious agency buyer would ask before granting OAuth access to client
  accounts — exactly the trust gap the audit flagged as highest-stakes for a brand-new domain with no
  other authority signals. / CONTENT: vendor-neutral framing throughout — leads with "MCP the protocol
  vs. the specific server you connect" (same distinction as asking "is HTTP secure?"), covers five real
  risk categories (data retention, OAuth scope breadth, local-vs-hosted servers, third-party/community
  package provenance, prompt injection as an active, non-ARLO-specific research area) and a 6-question
  vendor-vetting checklist before getting to ARLO's own answers. Every ARLO-specific claim reused only
  what's already corroborated on multiple *live* pages (checked via grep across `connectors.ts`,
  `comparisons.ts`, `services.ts` first): pass-through/no warehouse (5 live comparison pages), OAuth
  tokens encrypted at rest (5 live comparison pages — deliberately did NOT cite the specific
  "AES-256-GCM" cipher detail, since that only appears in `shared.ts`, confirmed dead/unwired code per
  the 2026-07-30 worklog entry, and isn't corroborated on any live page), per-user MCP tokens + audit
  log by user/client/timestamp (live `services.ts` agency-owner FAQ), one-click revoke leaving client
  platform data untouched (live FAQ pattern across connector pages). Explicitly disclosed one real
  limitation instead of glossing over it: ARLO doesn't publish a SOC 2 report or third-party security
  audit today — same "no invented certifications" caution as every prior comparison/content page (audit
  flagged the missing security page but never claimed ARLO had achieved specific certifications, so
  didn't invent one here either). Internal links: `/connect/google-analytics-mcp`,
  `/connect/search-console-mcp`, `/connect/google-ads-mcp`, `/blog/what-is-an-mcp-connector`, `/compare`.
  No manual relatedPosts wiring needed (automatic via `getAllBlogPosts()`). Did not add to
  `public/llms.txt` — same precedent as every prior post.
- VERIFY: `rm -rf .next && npm install && npm run build` — compiled successfully; same 9 pre-existing
  `(app)/*` + `/welcome` prerender failures as every prior session (missing `NEXT_PUBLIC_CONVEX_URL`,
  Vercel-only secret) — confirmed identical route list, not a regression. `/blog/[slug]` has no
  `generateStaticParams` (dynamic route, no static `.html` to grep), so verified directly via `npx
  tsx`: `getAllBlogSlugs()` includes the new slug, `getAllBlogPosts()` sorts it first (newest date,
  2026-08-17), `getBlogPostBySlug()` resolves title/tags/wordCount (1,727) correctly. `npm run lint`
  clean (only the same pre-existing unrelated `sitemap/page.tsx` CSS-tag warning). `git status` showed
  only `src/content/blog/manifest.json` (build-regenerated, reverted) + the new post — staged only the
  one intended file.

NEXT:
1. Human decisions still open (unchanged, now 10+ sessions): catalog.ts ETAs for meta_ads (3+ months
   overdue)/hubspot/mailerlite (both past due); the dormant 6-persona `route.ts` scaffold (open since
   2026-07-30); GSC submission (open since kickoff, 2026-07-04). Off-page Phase 1 still 0 of 6, most
   overdue item on the whole plan (flagged since Week 4) — needs a human with a browser, this sandbox
   has no outbound HTTP.
2. The ChatGPT-assistant-column connector-page variant flagged 2026-08-06 is still an open human-check
   item (confirm ChatGPT connector support is actually tested/live before building pages for it) — not
   shipping blind.
3. Content.md's original 12-post skeleton is now effectively exhausted of clear, non-redundant angles
   — 9 posts shipped total (100-questions, choquer-agency case study, windsor-ai install guide,
   what-is-an-mcp-connector, how-to-use-ai-for-seo, claude-mcp-server setup, client-reporting,
   agency-churn, mcp-server-security). The one item never separately shipped as its own post
   ("GA4-specific 3-minute setup") was judged redundant with `/connect/google-analytics-mcp` +
   `how-to-set-up-a-claude-mcp-server` rather than skipped by oversight — a future session should
   re-scope from a fresh keyword pull (SE Ranking `getRelatedKeywords`/`getLongTailKeywords` off
   Cluster 1/3 terms) rather than the original static skeleton before shipping post #10.
4. A weekly-pulse-style session (backlinks, CI status, catalog.ts re-check, full rank-position report)
   is due again — last full pulse was 2026-08-14, next one should land around 2026-08-21 per the
   established Friday cadence.

## 2026-08-13 (Thu) — Week 10/11: "Agency Client Churn Warning Signs" blog post — closes the most-overdue open content gap (flagged since 2026-07-16)

SHIPPED — `src/content/blog/posts/us/agency-client-churn-warning-signs.md` (new). Checked NEXT lists
across the last 5 sessions first: "How agency owners can spot churn risk 6 weeks before a client leaves"
(content.md §9 item 6) has been named as open in every worklog NEXT list since 2026-07-16 and was
re-flagged again 2026-08-10 alongside two other candidate posts (GA4 3-minute-setup, data-handling trust
piece) — picked this one because it's the single item repeated the most times without shipping, and
because it expands a real, already-live product claim rather than starting from nothing.
- PAGE: /blog/agency-client-churn-warning-signs (new) / CHANGE: new markdown post via the existing
  `src/content/blog/posts/us/*.md` → `getAllBlogPosts()` → `/blog/[slug]` pipeline (BlogPosting JSON-LD
  auto-generated, no template changes). 1,456 words (in range with the 7 existing posts). / TARGET:
  persona-anchored, not primarily keyword-volume driven — checked live via `DATA_getKeywordsMetrics` (US)
  first: `agency client churn`, `reduce client churn agency`, `client churn rate marketing agency`, `why
  do agency clients leave`, `spot client churn early` all `is_data_found: false`; only `agency churn rate`
  returned data (0/mo, KD 28) — same "no measurable volume yet, seed ahead of demand" situation the
  connector-matrix pages shipped into repeatedly, and consistent with content.md's own framing of this as
  a persona-page expansion, not a keyword-gap play. / WHY: closes the most-repeated open NEXT item on the
  whole worklog (7 sessions old) — connector matrix and comparison hub are both still fully caught up with
  no further items until a human unblocks catalog.ts (checked again this session: `meta_ads` still
  `coming_soon` eta "May 2026" now 3+ months overdue, `hubspot` eta "July 2026" now also past — both
  unchanged), so content remained the only human-independent lever. / CONTENT: built the piece around the
  real, already-live claim on `/services/agency-owner` (`src/content/services.ts`: `problemHeading: "Most
  churn is visible six weeks before it happens."`, plus the "Churn signals, surfaced early" benefit and
  "Churn that surprises you" pain point) — not inventing a new product claim, expanding an existing one
  into a full framework. Five concrete leading-indicator categories (performance trend, engagement
  drop-off, value-questioning language, internal-champion turnover, data/access requests), explicitly
  caveated as a rule of thumb, not a guarantee ("six weeks isn't a magic number" — FAQ #1) — same honesty
  pattern as every prior post's pricing/feature claims. No invented external statistics or cited "research"
  (no outbound web access to verify any churn-rate study, so didn't fabricate one — the framework is
  qualitative/operational, not stats-backed). ARLO's own role is scoped honestly: it answers the one
  systematizable signal (performance-trend across a whole client book) and is explicit in the FAQ that it
  doesn't predict or score churn — the other four signals are still a human account owner's judgment call.
  Internal links: `/services/agency-owner`, `/connect/google-analytics-mcp`, `/connect/google-ads-mcp`,
  `/connect/search-console-mcp`. No manual relatedPosts wiring needed (blog-to-blog links are automatic via
  `getAllBlogPosts()`). Did not add to `public/llms.txt` — same precedent as every prior post (llms.txt
  lists page types, not individual posts).
- ALSO CHECKED (informational, no action taken): SE Ranking position data for the 22 tracked keywords
  (project `askarlo.app`, site_id 12704162) — `avg_pos` still 100 (unranked) across all of 2026-08-01
  through today, unchanged from the 2026-08-07 pulse's 0/22 finding. Not surprising for a 6-week-old DA-0
  domain and not a new finding worth its own pulse entry — flagging the raw check here since it informed
  this session's targeting decision (no point re-optimizing already-shipped pages for rank movement that
  hasn't started yet); a dedicated weekly-pulse-style session should still do the fuller check (backlinks,
  CI, catalog.ts) rather than this narrow position-only pull.
- VERIFY: `rm -rf .next && npm install && npm run build` — compiled successfully; same 9 pre-existing
  `(app)/*` + `/welcome` prerender failures as every prior session (missing `NEXT_PUBLIC_CONVEX_URL`,
  Vercel-only secret) — confirmed identical route list, not a regression. `/blog/[slug]` has no
  `generateStaticParams` (dynamic route, no static `.html` to grep), so verified directly via `npx tsx`:
  `getAllBlogSlugs()` includes the new slug and sorts first (newest date, 2026-08-13),
  `getBlogPostBySlug()` resolves title/tags/wordCount (1,456) correctly. `npm run lint` clean (only the
  same pre-existing unrelated `sitemap/page.tsx` CSS-tag warning). `git status` showed only
  `src/content/blog/manifest.json` (build-regenerated, reverted) + the new post — staged only the one
  intended file.

NEXT:
1. Human decisions still open (unchanged, now 9+ sessions): catalog.ts ETAs for meta_ads (3+ months
   overdue)/hubspot (past due)/mailerlite; the dormant 6-persona `route.ts` scaffold (open since
   2026-07-30); GSC submission (open since kickoff, 2026-07-04). Off-page Phase 1 still 0 of 6, most
   overdue item on the whole plan (flagged since Week 4) — needs a human with a browser, this sandbox has
   no outbound HTTP.
2. The ChatGPT-assistant-column connector-page variant flagged 2026-08-06 is still an open human-check
   item (confirm ChatGPT connector support is actually tested/live before building pages for it) — not
   shipping blind.
3. A proper weekly-pulse-style session (backlinks, CI status, catalog.ts re-check, full rank-position
   report) is overdue — this session only did a narrow position-only check to inform targeting, not the
   fuller pulse format prior Friday sessions used.
4. Remaining open blog angles from `audits/content.md`'s 12-post skeleton: the GA4-specific 3-minute-setup
   piece, and "what actually happens to your clients' data when you connect an MCP tool" (item 12 — a
   dedicated trust/security piece, distinct from the passing security-FAQ mentions already live). The
   most-overdue item (churn-risk) is now closed as of this session.

## 2026-08-10 (Mon) — Week 10: "Client Reporting for Agencies" blog post — closes the single lowest-KD unaddressed keyword in the whole audit

SHIPPED — `src/content/blog/posts/us/client-reporting-for-agencies.md` (new), NEXT item 5 from
2026-08-06 ("one more blog post... e.g. the agency-owner churn-risk piece, or the GA4-specific
3-minute-setup piece"), but retargeted after re-checking `keyword-research.md` this session: `client
reporting` is **KD 6** — the single lowest-difficulty keyword anywhere in the audit (lower than `mcp
connector` KD 11) — explicitly called out since kickoff as needing a "Persona pillar page"
(keyword-research.md line 85, repeated in the Month-2 goal list at 2026-08-01) and never shipped in
any of the 11 prior content/page sessions. Checked the worklog (`grep -n "client reporting"`) to
confirm: it only ever appeared once, as a KPI term to watch for ranking movement, never as a page to
build.
- PAGE: /blog/client-reporting-for-agencies (new) / CHANGE: new markdown post through the existing
  blog pipeline (frontmatter → `getAllBlogPosts()` → `/blog/[slug]`, BlogPosting JSON-LD
  auto-generated, no template changes). 1,397 words (in range with the 6 existing posts, 1,149–2,264
  words after this session's expansion pass). / TARGET: primary **`client reporting`** (260/mo, KD 6,
  P1 per the 2026-07-04 keyword-research.md audit — not re-pulled live via SE Ranking this session,
  a content-only session with no page-status ambiguity to check) + secondary `client reporting tool`
  (140/mo, KD 18, P1), `marketing agency reporting`
  (170/mo, KD 14, P1), `seo agency reporting software` (260/mo, KD 16, P1) — four P1 "buyer's own
  vocabulary" terms from keyword-research.md §"Agency-reporting persona terms" in one post, none of
  which had a dedicated page. / WHY: the connector matrix (5/5 live Google sources) and comparison hub
  (5/5 named competitors) are both fully caught up with no further items until a human unblocks
  catalog.ts — confirmed catalog.ts unchanged again this session (meta_ads still coming_soon, eta "May
  2026", now 3+ months overdue; hubspot/mailerlite both past their July eta). Content was the only
  human-independent lever left, and this closes the most overdue specific gap on the whole keyword
  list rather than a generic "one more post." / CONTENT: honest three-way breakdown of how agencies
  actually handle client reporting today (manual spreadsheets, dashboard tools like Looker
  Studio/AgencyAnalytics/Whatagraph, live conversational reporting) — did not claim dashboards are
  obsolete; explicitly says a dashboard is still the better call when a client contractually expects a
  branded portal. A "what a client reporting tool needs to actually do" checklist and a concrete
  time-cost section (est. 20 min/client/week × 15 clients = ~5 hrs/week, explicitly labeled as
  illustrative math, not a product claim) ground the piece instead of just restating ARLO's feature
  list. SEO-specific section links to the existing `search-console-mcp` connector page and
  `seo-specialist` service page rather than duplicating their content. No fabricated competitor
  pricing (same caution as every prior comparison/content page — no outbound web access to verify).
  Only reused already-live, verified claims for ARLO itself (free during early access, unlimited
  clients/sources/team, no card — matches `services.ts` line 672 and the 2026-07-18 product commit).
  Internal links: `/connect/google-analytics-mcp`, `/connect/google-ads-mcp`,
  `/connect/search-console-mcp`, `/services/seo-specialist`, `/compare`. No manual relatedPosts
  wiring needed (blog-to-blog links are automatic via `getAllBlogPosts()`, same as every prior post).
  Did not add to `public/llms.txt` — same precedent as every prior post (llms.txt lists page types,
  not individual posts; none of the 6 existing posts are listed there either).
- VERIFY: `rm -rf .next && npm install && npm run build` — compiled successfully; same 9 pre-existing
  `(app)/*` + `/welcome` prerender failures as every prior session (missing `NEXT_PUBLIC_CONVEX_URL`,
  Vercel-only secret) — confirmed identical route list, not a regression. `/blog/[slug]` has no
  `generateStaticParams` (dynamic route, no static `.html` to grep), so verified directly via `npx
  tsx`: `getAllBlogSlugs()` includes the new slug and sorts first (newest date, 2026-08-10),
  `getBlogPostBySlug()` resolves title/tags/wordCount (1,397) correctly. `npm run lint` clean (only
  the same pre-existing unrelated `sitemap/page.tsx` CSS-tag warning). `git status` showed only
  `src/content/blog/manifest.json` (build-regenerated, reverted) + the new post — staged only the one
  intended file.

NEXT:
1. Human decisions still open (unchanged, now 8+ sessions): catalog.ts ETAs for meta_ads (3+ months
   overdue)/hubspot/mailerlite; the dormant 6-persona `route.ts` scaffold (open since 2026-07-30); GSC
   submission (open since kickoff, 2026-07-04). Off-page Phase 1 still 0 of 6, most overdue item on
   the whole plan (flagged since Week 4) — needs a human with a browser, this sandbox has no outbound
   HTTP.
2. The ChatGPT-assistant-column connector-page variant flagged 2026-08-06 is still an open human-check
   item (confirm ChatGPT connector support is actually tested/live before building pages for it) — not
   shipping blind.
3. Next weekly pulse should re-check the SE Ranking position data for the 22 tracked keywords (last
   checked 2026-08-07: 0/22 ranked, expected at the one-week mark) for the first real week-over-week
   comparison.
4. More blog posts remain open in `audits/content.md`'s 12-post skeleton with genuinely new angles not
   yet covered: the agency-owner churn-risk piece (item 6 in the skeleton, flagged repeatedly since
   2026-07-16, still not shipped), the GA4-specific 3-minute-setup piece, and "what actually happens to
   your clients' data when you connect an MCP tool" (item 12 — a dedicated trust/security piece, distinct
   from the passing security-FAQ mentions already live).

## 2026-08-07 (Fri) — Weekly SEO pulse: first real rank-check data in — 0 of 22 tracked keywords ranked in top 100 after a full week of daily checks (expected for a DA-4, 5-week-old domain with 1 referring domain, but Month 2 KPI #1 not on track at week 1); catalog.ts hubspot/mailerlite ETAs now also past due (meta_ads still 3mo overdue); CI 17/18 green, off-page still 0/6, GSC/GA4 still pending — see reports/outbox/2026-08-07-weekly-pulse.json.

## 2026-08-06 (Thu) — Week 9 long-tail: "How to Set Up a Claude MCP Server" blog post + llms.txt gap fix

SHIPPED — the NEXT item 1 from 2026-08-03 ("worth scoping next session": a `claude mcp server` pillar
page, since the connector matrix is fully caught up and off-page/human items remain blocked). Chose
the blog-post route over a new page type: `claude mcp server` is CLUSTER-1's own top P1 term
(keyword-research.md: 810/mo, KD 21, "core product/how-to page") and content.md §8 independently lists
"How to set up an MCP server for Claude Desktop" as an open content gap — one post closes both, using
existing, already-schema'd blog infra rather than building new page-type risk.
- PAGE: /blog/how-to-set-up-a-claude-mcp-server (new) / CHANGE: new markdown post via the existing
  `src/content/blog/posts/us/*.md` → `getAllBlogPosts()` → `/blog/[slug]` pipeline (BlogPosting JSON-LD
  auto-generated, no template changes). ~1,600 words (in range with the 5 existing posts, 1,435–2,333
  words). / TARGET: primary **`claude mcp server`** (810/mo, KD 21, P1 per keyword-research.md — highest
  single search volume of any term this routine has targeted so far) + secondary `mcp server for claude`
  (90/mo, KD 28), `claude code mcp server setup` (50/mo, KD 24), `how to install mcp` (30/mo, KD 26),
  all P1/P2 CLUSTER-1 terms. / WHY: distinct angle from the existing `what-is-an-mcp-connector.md` post
  (checked first to avoid redundancy) — that post is conceptual ("what is MCP, how is it different from
  an API/dashboard tool"); this one is the practical how-to content.md's own gap analysis called for:
  remote-vs-local MCP server setup mechanics, the actual Claude Desktop Settings → Connectors steps,
  Claude Code/other-client portability, a troubleshooting section (disconnected connector, stale
  conversation not picking up a new tool, local server PATH issues, expired OAuth tokens), and a
  "what to check before connecting a marketing MCP server to client data" section that leads into ARLO's
  5 live Google connectors. No fabricated CLI syntax for Claude Code (checked — didn't invent flags I
  wasn't certain of; told the reader to check the client's own current docs instead, since MCP client
  config surfaces change between versions) and no invented product claims — every ARLO-specific claim
  (one OAuth grant, 5 connectors, read-only/pass-through, 5-step flow) matches what's already live on the
  connector pages and `processSteps` in `shared.ts`. Internal links to `what-is-an-mcp-connector`, all 5
  live `/connect/*` pages, and `/compare`.
- FIX (found while cross-checking the new post's ARLO connector links against `public/llms.txt`,
  unrelated to the post itself): `llms.txt`'s Pages list was missing the Google Business Profile MCP
  connector page shipped 2026-08-03 — that session's own worklog entry didn't mention an llms.txt update,
  and it wasn't one; the other 4 live connector pages were all listed, GBP wasn't. One-line addition,
  same pattern as every prior connector-page session ("added the new connector page to the Pages list").
  Did NOT add the new blog post itself to llms.txt — confirmed precedent from the 2026-07-23 entry still
  holds: llms.txt lists page *types* (Blog hub, connector pages, compare pages), not individual posts;
  none of the 6 (now 6) posts are listed there individually, so adding this one alone would be
  inconsistent.
- VERIFY: `rm -rf .next && npm install && npm run build` — compiled successfully; same 9 pre-existing
  `(app)/*` + `/welcome` prerender failures as every prior session (missing `NEXT_PUBLIC_CONVEX_URL`,
  Vercel-only secret) — confirmed identical route list, not a regression. `/blog/[slug]` has no
  `generateStaticParams` (dynamic route, same as `/compare/[slug]` in prior sessions — no static `.html`
  to grep), so verified directly via `npx tsx`: `getAllBlogSlugs()` includes the new slug, sorts first
  (newest date), `getBlogPostBySlug()` resolves title/tags/wordCount correctly. `npm run lint` clean
  (only the same pre-existing unrelated `sitemap/page.tsx` CSS-tag warning). `git status` showed only
  `public/llms.txt` + the new post touched after reverting the build-regenerated
  `src/content/blog/manifest.json` diff (same pattern as every prior session) — staged only the 2
  intended changes.

NEXT:
1. Week 9's other long-tail item — a "ChatGPT" assistant-column variant of the 5 existing connector
   pages — still open. Checked this session: nothing in the codebase (`src/lib/connectors/`,
   `src/content/connectors.ts`) indicates ARLO's MCP server is validated against ChatGPT's connector/
   plugin surface specifically (vs. "any MCP-compatible client" in the existing FAQ copy) — before
   building ChatGPT-specific landing pages, worth a human confirmation that ChatGPT connector support is
   actually tested/live, not just theoretically MCP-compatible, to avoid overclaiming the way the
   Meta Ads catalog.ts gap almost did. Flagging as a human-check item, not shipping blind.
2. Off-page Phase 1 (Smithery.ai, Glama.ai, mcp.so, awesome-mcp-servers, AlternativeTo, SaaSHub) still
   0 of 6 — most overdue item on the whole plan (flagged since Week 4), still needs a human with a
   browser (this sandbox has no outbound HTTP).
3. Human decisions still open (unchanged): catalog.ts ETAs for meta_ads/hubspot now well past due; the
   dormant 6-persona `route.ts` scaffold (open since 2026-07-30); GSC submission (open since kickoff,
   2026-07-04).
4. Check SE Ranking position-check results for the 22 tracked keywords (project `askarlo.app`, wired up
   2026-08-01) — should have real data by now; report it in the next weekly-pulse-style session rather
   than this one (this session was scoped to shipping content, not a full pulse).
5. One more blog post with a genuinely new angle queued in `audits/content.md`'s 12-post plan (e.g. the
   agency-owner churn-risk piece, or the GA4-specific 3-minute-setup piece) — still open.

## 2026-08-03 (Mon) — Connector matrix: shipped the 5th (final Google) connector page — Google Business Profile MCP

SHIPPED — closed a gap that's been sitting open since Week 4 of the 12-week-strategy.md plan
("YouTube MCP + Business Profile MCP — completes the 6 Google sources"): YouTube shipped
2026-07-16, Business Profile never did. Checked `src/lib/connectors/catalog.ts` first — `gbp`
(Google Business Profile) is `status: "live"` in the product today (not blocked on a human
decision like Meta Ads/HubSpot/Salesforce), so this was buildable now, no catalog dependency.
- PAGE: `/connect/google-business-profile-mcp` (new) / CHANGE: added a 5th entry to
  `src/content/connectors.ts` following the exact template of the 4 existing connector pages —
  TL;DR, 3-step Google OAuth connect flow, 5 example prompts, real metrics/dimensions
  (search/maps impressions, call clicks, direction requests, website clicks, review count —
  scoped to what the Business Profile Performance API actually returns), 5 FAQs including an
  honest "not yet supported" one (no review replies / listing edits, read-only reporting only,
  matching the honesty pattern of the Google Ads/Search Console pages' own "not yet supported"
  FAQs). Full `WebPage` + `HowTo` + `FAQPage` JSON-LD via the shared `/connect/[slug]/page.tsx`
  template (verified rendered in the built HTML, not just written). / TARGET: primary
  "Google Business Profile MCP" (rides the same emerging, fragmented, zero-competition MCP
  cluster as the other 4 connector pages — checked SE Ranking: like "search console mcp" (30/mo)
  and unlike "google ads mcp" (590/mo), this exact phrase has genuinely zero recorded search
  volume yet, `is_data_found: false` — too new, not a research gap; the real current demand it
  captures in-copy is "google business profile api" at 390/mo, KD 14, US) / WHY: this is the
  literal last of the 6 Google-source connector pages the strategy's Week 2-4 playbook called
  for, was flagged as still-missing in this session's own read of `12-week-strategy.md`, and
  every one of the other 4 live connector pages' own copy already promises "the same grant also
  unlocks ... Business Profile" — those were pointing at a page that didn't exist until now.
- ALSO: added `google-business-profile-mcp` to `relatedConnectorSlugs` on all 4 existing
  connector pages (GA4, Google Ads, Search Console, YouTube) so the internal-link matrix is
  bidirectional, not just the new page linking out. relatedServiceSlugs: `account-manager` +
  `agency-owner` (multi-location/local-listing reporting fits those personas, matching the
  precedent set by youtube-mcp). relatedComparisonSlugs: `agencyanalytics-vs-arlo` (AgencyAnalytics
  markets GBP/local reporting as a feature — more topically relevant than Supermetrics here).
- VERIFY: `rm -rf .next && npm install && npm run build` — compiled successfully, exit 0; same
  9 pre-existing `(app)/*` + `/welcome` prerender failures as every prior session (missing
  `NEXT_PUBLIC_CONVEX_URL`, Vercel-only secret) — not a regression. Confirmed in the built output:
  `.next/server/app/connect/google-business-profile-mcp.html` exists, contains
  `"@type":"FAQPage"` and canonical `https://askarlo.app/connect/google-business-profile-mcp`
  (not the homepage — canonical bug stayed fixed). `sitemap.xml` picked up the new URL
  automatically (data-driven from `getAllConnectorSlugs()`, no sitemap.ts edit needed).
  `npm run lint` clean (same pre-existing unrelated `sitemap/page.tsx` CSS-tag warning as every
  prior session). `git status` showed only `src/content/connectors.ts` touched after reverting
  the build-regenerated `blog/manifest.json` diff (same pattern as every prior session) — staged
  only the one intended file.

NEXT:
1. **Connector matrix is now fully caught up to the 6-Google-source plan** (GA4, Google Ads,
   Search Console, YouTube, Business Profile all live) — no further connector pages queued until
   a currently-`coming_soon`/`waitlist` platform in `src/lib/connectors/catalog.ts` flips to
   `live` (Meta Ads, HubSpot, Shopify, Stripe are the closest ETAs). Per the strategy's own Week 9
   note, the next connector-matrix move is the long-tail variant angle instead: a `claude mcp
   server` pillar page, or a "ChatGPT" assistant-column variant of the existing 5 pages — worth
   scoping next session.
2. Off-page Phase 1 (Smithery.ai, Glama.ai, mcp.so, awesome-mcp-servers, AlternativeTo, SaaSHub)
   is still 0 of 6, now the single most overdue item across the whole plan (flagged since Week 4,
   still blocked — this sandbox has no outbound HTTP and this session's GitHub MCP scope is
   choquer-agency/arlo only, so directory signups genuinely need a human with a browser).
3. Human decisions still open (unchanged from 2026-08-01): catalog.ts ETAs for meta_ads/hubspot
   now past due in their own copy; the dormant 6-persona `route.ts` scaffold (open since
   2026-07-30); GSC submission (open since kickoff, 2026-07-04).
4. Check SE Ranking position-check results for the 22 tracked keywords (project `askarlo.app`,
   site_id 12704162, wired up 2026-08-01) — first real data should be in by now; report it in the
   next weekly pulse instead of this session (this session was scoped to shipping the page, not a
   full pulse).
5. Two more blog posts with genuinely new angles queued in `audits/content.md` — still open.

## 2026-08-01 (Sat) — Monthly deep-dive: Month 1 review (July 2026) — SCORED + rank tracking finally wired up

SCORE — Month 1 goal: **Partial** (2 of 4 Month-1 KPIs from 12-week-strategy.md clean, 1 fixed on
the literal last day of the month, 1 still blocked on a human). Content production and technical-debt
remediation were genuinely strong: 10 net-new indexable pages/posts shipped across 8 build-gated
sessions (4 connector pages, 5 comparison pages closing the audit's full named-competitor list, a
`/compare` hub, 1 blog post), zero regressions, plus two rounds of real stale-pricing trust-bug fixes
on already-live pages — all ahead of the ~2-pages/week quality bar. But the plan's own measurement
infrastructure (SE Ranking rank tracking) sat unbuilt for the entire month despite being flagged in
4 consecutive weekly pulses (since 2026-07-10) as "needs a human to wire up" — it did not, in fact,
need a human. See below.

ACTION TAKEN THIS SESSION (not just reporting — fixed the standing blocker): checked whether the SE
Ranking MCP connector's write tools could create a rank-tracking project directly, since every prior
pulse assumed this needed a human with SE Ranking dashboard access. It didn't — `PROJECT_createProject`
+ `PROJECT_addSearchEngine` + `PROJECT_addKeywords` are all available in this sandbox. Created project
`askarlo.app` (site_id 12704162), Google US daily checks, and added the 22 keywords that map to all
10 pages shipped this month (each with its real live URL as `target_url` — every connector page, every
comparison page, the blog post, and the 3 persona pages carrying Cluster-3 terms), then queued the
first position check immediately (`PROJECT_runPositionCheck`) rather than waiting on the daily
schedule. Results were not back by the time this report was written (SE Ranking checks run
asynchronously) — no position numbers fabricated; first real data expected before the next weekly
pulse. **Lesson for future sessions: before flagging something "needs a human," check whether the
connector's own write/PROJECT_* tools can do it — this specific gap survived 4 pulses on a false
assumption.**

ALSO PULLED LIVE THIS SESSION: `DATA_getBacklinksSummary` (askarlo.app, domain mode) — 2 backlinks /
1 referring domain, both from `futurlabs.dev` (the sister portfolio site linking to Arlo as a
project — a normal existing cross-link, not off-page link-building). Off-page Phase 1 (Smithery.ai,
Glama.ai, mcp.so, awesome-mcp-servers, AlternativeTo, SaaSHub — scheduled Week 4 in
12-week-strategy.md) is still at 0 of 6, now the most overdue content-adjacent item on the list.
`DATA_getAiSearchOverview` (US) — still null brand_presence, 0 link_presence, 0 ai_opportunity_traffic,
unchanged from every prior pulse. SE Ranking's own `domain_trust` for the new project: 4 (new-domain
baseline, consistent with the audit's DA-0 framing).

Full monthly report (leads/CPL proxy, what's working/not, numbers-vs-targets table, Month 2 goal) —
see `reports/outbox/2026-08-01-monthly.json`. GA4 (`G-CP22NBPLFP`) still firing since 07-04 but
conversions remain unqueryable from this sandbox (no outbound GA4 API access) — leads/CPL stays
"pending" per the strategy doc's own honest frame (new domain, month-1/2 goals are leading indicators,
not leads).

GOAL — Month 2 (August 2026), set this session:
1. First measured SERP position for ≥15 of the 22 newly-tracked keywords (baseline — any position,
   top-50 is not assumed for a brand-new domain in week 1 of tracking).
2. At least 3 of the lowest-KD tracked terms (`mcp connector` KD 11, `client reporting` KD 6,
   `agencyanalytics alternative`/`whatagraph alternative` KD 7) entering the top-50.
3. Off-page Phase 1 shipped — referring domains from 1 to 6+ via directory/registry listings
   (Smithery.ai, Glama.ai, mcp.so, awesome-mcp-servers GitHub PR, AlternativeTo, SaaSHub).
4. Human decisions closed: catalog.ts/Meta Ads + 8-connector status (open 5 sessions now, since
   2026-07-20), the dormant 6-persona `route.ts` scaffold (open since 2026-07-30), and GSC submission
   requested (open since kickoff, 2026-07-04 — still needs human Search Console access, this sandbox
   has no outbound HTTP to self-serve it).
5. Leads/CPL proxy (GA4) stays "pending" through Month 2 by design — becomes the goal metric once
   pages are actually ranking, not before.

## 2026-07-31 (Fri) — Weekly SEO pulse: SE Ranking rank-tracking gap now 4th pulse running (9 pages/posts shipped, zero rank visibility) + catalog.ts past its own eta dates on meta_ads/tiktok_ads/hubspot/mailerlite; CI 13/14 green, GSC/GA4 still pending — see reports/outbox/2026-07-31-weekly-pulse.json.

## 2026-07-30 (Thu) — Week 8: pricing-drift fix on 2 more live pages (persona pages this time) + a routing finding

CONTEXT: session started with a stale local `main` ref (this container's `main` was 2 commits behind
`origin/main` — a plain `git checkout main` would have silently rolled back the 07-27 Funnel.io +
pricing-fix commits). Caught it via `git fetch origin main` + `git merge --ff-only` before doing any
work — flagging in case this recurs; future sessions should fetch before trusting a local branch ref.

SHIPPED — fixed the same class of stale-pricing bug the 2026-07-27 session fixed on 2 comparison
pages, found this session on 2 **persona** pages instead: `/services/solo-business-owner` and
`/services/agency-owner`.
- PAGE: /services/solo-business-owner / CHANGE: `src/content/services.ts` (the actual live source —
  see routing finding below) still advertised the retired tier structure: meta description/og/twitter
  said "$19/mo", the hero subhead said "just a $19/mo AI layer", a benefits card said "$19/mo, 7 source
  types, 2,500 queries", the signup step said "14-day trial, no card", and two FAQs said "Upgrade to
  Studio ($99/mo) when you add a 2nd client" and "Studio or Agency unlock higher source limits." All of
  this predates the 2026-07-18 "early access: free = unlimited clients/sources/team" product commit and
  contradicts the live `/pricing` page and `billing.ts` (`free: { clients: Infinity, sourceTypes:
  Infinity, teamMembers: Infinity }`). Rewrote every instance to the same "free during early access, no
  card" framing already established on `/pricing` and the 2 comparison pages fixed last session — no
  invented numbers, no new claims, just removed the false ones. / TARGET: accuracy/trust on an
  already-indexed, high-intent persona page (a prospect landing here from "AI business analytics"
  searches and then checking `/pricing` would have seen two different pricing stories) / WHY: same
  category of live-site trust issue the audit's own content.md flagged in §7 ("pricing model
  inconsistency") — this is the persona-page half of that finding that never got fixed; the 07-27
  session only caught the comparison-page half.
- PAGE: /services/agency-owner / CHANGE: `src/content/services.ts` FAQ "How does pricing work as the
  team grows?" said "ARLO is priced per seat with agency plans for 10+ users" — this is the *exact*
  contradiction the original 2026-07-04 audit (content.md §7) flagged between the agency-owner page's
  per-seat claim and the "priced per business tracked" model stated everywhere else, still unfixed 4
  weeks later. Replaced with the free-during-early-access framing. / TARGET: same accuracy/trust
  rationale as above, plus this one closes a specific audit finding that's been open since kickoff.
- ROUTING FINDING (worth a human look): each of the 6 persona slugs also has a parallel, unused
  `src/app/services/{slug}/route.ts` + `_content/{slug}.ts` + `_shell.html` scaffold (same
  route.ts+_shell.html+_content pattern as `/compare` and the homepage). I initially edited the
  solo-business-owner and agency-owner versions of *that* scaffold first, assuming (per the "Redesign
  rollout" commit `009111a` pattern used elsewhere) it was the live template — it is not. Confirmed via
  a clean `rm -rf .next && npm run build` + grepping the static output: `/services/solo-business-owner`
  and `/services/agency-owner` render from `src/app/services/[slug]/page.tsx` +
  `src/content/services.ts` (old dashboard-era template), not the route.ts scaffold. The 6 scaffold
  folders look like an abandoned or in-progress migration — accurate-but-inert content sitting next to
  the real, live, still-inaccurate template. Fixed the pricing in *both* places (the live
  `services.ts` and the dormant scaffold) so whichever one is real going forward is already correct,
  but flagging this for a human: either finish promoting the 6 persona pages to the route.ts pattern
  (matching how `/about`, `/pricing`, `/blog`, `/contact`, `/compare` were already promoted per
  `src/app/sitemap/page.tsx`'s own tracker) or delete the dead scaffold — right now it's a
  maintenance trap where a future session could "fix" content that never renders (as I almost did).
- NOT touched: `src/content/shared.ts` also has the same stale $19/$99/$249/$499 tier data (a
  `pricingTiers` array + several FAQ entries) — confirmed this file is dead code too (not imported by
  the live homepage, `src/app/route.ts` → `arlo/_content/home.ts`, which has zero pricing-tier
  mentions at all). No live-page impact, so left as-is rather than editing unused code; noting here in
  case a future session wires it back up and needs to know it's stale first.
- VERIFY: `npm install && npm run build` (full clean rebuild, `rm -rf .next` first to rule out cache)
  — compiled successfully; same 9 pre-existing `(app)/*` + `/welcome` prerender failures as every prior
  session (missing `NEXT_PUBLIC_CONVEX_URL` in this sandbox, Vercel-only secret) — not a regression.
  `npm run lint` clean (only the same pre-existing unrelated `sitemap/page.tsx` CSS-tag warning).
  Grepped the built static HTML directly (`.next/server/app/services/{solo-business-owner,
  agency-owner}.html`) for `$19|$99|per seat|14-day trial` — zero matches after the fix, vs. several
  before; confirmed the new "free during early access" copy renders in its place. `git status` shows
  only the 5 intended source files touched — reverted the build-regenerated
  `src/content/blog/manifest.json` diff before staging (same pattern as every prior session).

NEXT (Week 8, see 12-week-strategy.md):
1. **Get a human answer on catalog.ts** for the 9 connectors in the Jul 18 product commit — now open
   5 sessions (since 2026-07-20). `meta_ads` eta "May 2026" and `hubspot` eta "July 2026" are now both
   past due in the catalog copy itself (checked again this session, still unchanged: `meta_ads`/
   `hubspot`/`mailchimp`/`mailerlite` still `coming_soon`, `salesforce`/`pipedrive` still `waitlist`,
   `gohighlevel`/`clarity`/`gtm` still absent from the catalog entirely). Blocks Meta Ads MCP and 8
   other potential connector-matrix pages.
2. **Resolve the routing finding above** — either finish wiring the 6 `route.ts`/`_content`/
   `_shell.html` persona scaffolds into real routes (needs a product/eng decision on why they exist
   and whether the migration was intentionally paused) or delete them. Needs a human call, not a
   content-writing session.
3. Two more blog posts with genuinely new angles (not redundant with the 5 live posts): a
   GA4-specific how-to ("GA4 in Claude: the 3-minute setup") and/or a persona-anchored piece from
   `audits/content.md`'s list (e.g. agency-owner churn-risk piece).
4. Comparison hub is fully caught up to the audit's named-competitor list (Windsor.ai, Supermetrics,
   AgencyAnalytics, Whatagraph, Funnel.io all live) — no further comparison pages queued until a new
   named competitor surfaces in a future audit refresh.
5. Still open (deferred 9x now): static-render the marketing pages (move
   `ConvexAuthNextjsServerProvider` into an app-only route group). Test auth end-to-end after.
6. Submit sitemap + all new pages in GSC (needs a human with Search Console access — unchanged). No
   SE Ranking rank-tracking project for askarlo.app yet either (flagged since 2026-07-10) — worth a
   human wiring one up so future sessions can check real ranking movement instead of keyword-
   difficulty snapshots.

## 2026-07-27 (Mon) — Week 7/8: `/compare/funnel-vs-arlo` (closes the comparison-hub gap) + pricing-drift fix on 2 live pages

SHIPPED — `/compare/funnel-vs-arlo` (new), the NEXT item 3 named in the 2026-07-23 entry: "the
one named competitor still without a page" per the audit's comparison-hub target list.
- PAGE: /compare/funnel-vs-arlo (new) / CHANGE: new entry in `src/content/comparisons.ts`, same
  `/compare/[slug]` template as the other 4 comparison pages (no template changes — confirmed via
  `getAllComparisonSlugs()`/`getComparisonBySlug()` that it's picked up automatically, so it
  auto-appears on `/compare`, in `sitemap.ts`, and needs no route/hub-page edits). / TARGET:
  **`funnel.io`** — re-checked live via `DATA_getKeywordsMetrics` (US) before writing: 170/mo, KD
  17, competition 0.59, steady volume all year — the audit's own P2 comparison-hub target and the
  last of the 6 named direct/adjacent competitors (`competitor.md` §2) without a dedicated page.
  Secondary: `funnel io pricing` (90/mo, KD 23 — surprisingly high-intent; Funnel.io doesn't
  publish self-serve pricing, so people search for it directly) and `funnel.io alternative`
  (10/mo, KD 17). / WHY: only remaining comparison-hub item with no human dependency — item 1
  (catalog.ts) still needs a product-owner decision (checked again this session: `meta_ads` is
  still `coming_soon` with eta "May 2026", already past — unchanged, now 4 sessions blocked); the
  other Week-7 NEXT items (2 more blog posts, static-render infra change) were flagged as lower
  priority or needing their own dedicated session. / CONTENT: positioned Funnel.io accurately per
  `competitor.md`'s own profile ("marketing data hub / ETL to BI + warehouse", DA 89) —
  differentiated from the existing Windsor.ai and Supermetrics pages (same broad ETL category) by
  Funnel's actual distinguishing traits: normalizing data into one model for a downstream BI tool
  (Looker Studio/Tableau/Power BI) someone still has to build a report in, and no public self-serve
  pricing (quote-only) vs. ARLO's free-to-start-today positioning — not a redundant restate of the
  Windsor page's "no warehouse" angle. No invented pricing numbers for Funnel.io (no outbound web
  access to verify exact figures) — described qualitatively ("custom quote — no public self-serve
  pricing"), same caution pattern as every prior comparison page. Table/FAQ/whenCustom/whenSaas
  sections match the `ComparisonPage` TS interface exactly (verified via `tsx` import — see VERIFY).
- FIX (found while reviewing comparisons.ts, unrelated to the new page but same file + same
  accuracy category as the audit's P0 concerns): `agencyanalytics-vs-arlo` and `whatagraph-vs-arlo`
  both still advertised ARLO's own price as "$0 Free / $19 Solo / $99 Studio" with FAQ text citing
  a "$99/mo Studio" tier. That tier structure no longer exists — the 2026-07-18 "Early access: free
  plan = unlimited clients/sources/team" product commit made everything free with no paid tiers,
  and the live `/pricing` page + the `/compare` hub's own FAQ both now say "free during early
  access, no card" with zero tier language. Two live comparison pages were advertising a paid tier
  a prospect can't actually buy — corrected both the table row and FAQ answer on each page to match
  the current live pricing story ("Free during early access — no card"). Confirmed no other file
  references the stale tiers (`grep` for `$19|$99|Solo|Studio` across content/blog — clean after
  the fix).
- SITE: `public/llms.txt` — added "Funnel.io vs. ARLO" to the Pages list (same pattern as every
  prior connector/comparison page).
- VERIFY: `npm install && npm run build` — compiled successfully, all 86 static pages generated;
  confirmed via `git stash` + rebuild on unmodified `main` that the export-phase failures on
  `/admin`, `/clients`, `/connections`, `/dashboard`, `/settings/billing`, `/settings/mcp`,
  `/solo-dashboard`, `/team`, `/welcome` are byte-for-byte identical to baseline (missing
  `NEXT_PUBLIC_CONVEX_URL` in this sandbox, Vercel-only secret — not a regression). `npm run lint`
  clean (only the same pre-existing unrelated `sitemap/page.tsx` CSS-tag warning). Since
  `/compare/[slug]` is a dynamic route (not statically prerendered — no `.html` to grep), verified
  the new content directly: `npx tsx` import of `comparisons.ts` confirms `funnel-vs-arlo` is in
  `getAllComparisonSlugs()`, all required `ComparisonPage` fields are present (4 FAQs, 7 table
  rows, valid `serviceSlug: "account-manager"`), and the shared `/compare/[slug]/route.ts` renderer
  is 100% generic (same function already renders the other 4 live comparison pages) — no
  page-specific rendering risk. `git status` shows only `src/content/comparisons.ts` and
  `public/llms.txt` touched — reverted the build-regenerated `src/content/blog/manifest.json` diff
  before staging (same pattern as every prior session).

NEXT (Week 8, see 12-week-strategy.md):
1. **Get a human answer on catalog.ts** for the 9 connectors in the Jul 18 product commit — now
   open 4 sessions (since 2026-07-20). `meta_ads` eta "May 2026" is now 2+ months past due in the
   catalog copy itself. Blocks Meta Ads MCP and 8 other potential connector-matrix pages.
2. Two more blog posts with genuinely new angles (not redundant with the 5 live posts): a
   GA4-specific how-to ("GA4 in Claude: the 3-minute setup") and/or a persona-anchored piece from
   `audits/content.md`'s list (e.g. agency-owner churn-risk piece).
3. Comparison hub is now fully caught up to the audit's named-competitor list (Windsor.ai,
   Supermetrics, AgencyAnalytics, Whatagraph, Funnel.io all live) — no further comparison pages
   queued until a new named competitor surfaces in a future audit refresh.
4. Still open (deferred 8x now): static-render the marketing pages (move
   `ConvexAuthNextjsServerProvider` into an app-only route group). Test auth end-to-end after.
5. Submit sitemap + all new pages in GSC (needs a human with Search Console access — unchanged). No
   SE Ranking rank-tracking project for askarlo.app yet either (flagged since 2026-07-10) — worth a
   human wiring one up so future sessions can check real ranking movement instead of keyword-
   difficulty snapshots.

## 2026-07-24 (Fri) — Weekly SEO pulse: SE Ranking rank-tracking gap now 3 pulses old (7 pages/posts shipped with zero rank visibility) + catalog.ts Meta Ads decision still open 6 sessions; CI 10/11 green, GSC/GA4 still pending — see reports/outbox/2026-07-24-weekly-pulse.json.

## 2026-07-23 (Thu) — Week 6/7: first foundational blog post — "What Is an MCP Connector?"

SHIPPED — `src/content/blog/posts/us/what-is-an-mcp-connector.md` (new), the NEXT item 3 blog-engine
task from 2026-07-20, but retargeted from the literal 12-week-strategy.md post list ("What is an MCP
server (for marketers)") to the term the live keyword data actually supports for a new domain.
- PAGE: /blog/what-is-an-mcp-connector (new) / CHANGE: new markdown post through the existing
  `src/content/blog/index.ts` pipeline (frontmatter → `getAllBlogPosts()` → `/blog/[slug]` template,
  `BlogPosting` JSON-LD auto-generated, no template changes). Discovered the blog engine itself was
  already shipped by a product commit (`009111a`, Jul 9 "Redesign rollout") with 4 real posts already
  live — `audits/content.md`'s "blog is completely empty" finding is now stale; confirmed `/blog`
  route.ts only falls back to its hardcoded `SAMPLE` placeholder cards when `getAllBlogPosts()` returns
  zero real posts, so no fabricated-content risk there. / TARGET: **`mcp connector`** — re-checked live
  via `DATA_getKeywordsMetrics` (US) before writing: KD **11** (unchanged from the 07-04 audit), 210/mo,
  competition 0.02, steady 3x growth over 12mo (70→210) — the README/12-week-strategy's own explicit
  "own this" P1 call, and the one CLUSTER-1 term with no dedicated page yet (checked all 4 existing
  posts — "mcp connector" appears only incidentally, no cannibalization). Secondary: `mcp for marketing`
  (KD 9, 10/mo, exact-fit positioning term per the audit). Deliberately did **not** target the literal
  strategy-doc title "what is mcp server" / "model context protocol" (KD 70–95, P3 "unwinnable for a
  new domain in 3-6 months" per the audit's own framing) — same rationale the audit itself gives. /
  WHY: highest-value NEXT item achievable this session — item 1 (catalog.ts confirmation) and item 4
  (GSC/SE Ranking project) both explicitly need a human; item 2 (static-render marketing pages) is a
  repeat-deferred infra/auth-risk change flagged 7x now as needing its own dedicated session, not worth
  rushing under "never break the live site." Blog content had zero human dependency and directly serves
  the routine's standing FOCUS instruction (low-KD MCP-integration cluster). / CONTENT: plain-English
  definitional piece — what an MCP connector is, how it differs from a dashboard tool (Looker Studio)
  and a pipeline/API integration (Windsor.ai, Supermetrics — Arlo's own named competitors), a security/
  "is it safe for client data" FAQ answer (pass-through model, no data at rest), and a setup walkthrough
  matching the real 5-step OAuth flow in `src/content/shared.ts` (`processSteps`). No invented pricing —
  only reused the one verified public claim already live elsewhere ("free right now in early access, no
  card required," matches `billing.ts`/07-18 free-plan commit). Internal links to all 4 live connector
  pages (`/connect/google-analytics-mcp`, `/connect/google-ads-mcp`, `/connect/search-console-mcp`,
  `/connect/youtube-mcp`), the `/compare` hub, and `/welcome` CTA — no orphan risk; new post also
  auto-appears in the "More articles" block on all 4 existing posts (template pulls from
  `getAllBlogPosts()`, no manual relatedPosts wiring needed for blog-to-blog links, unlike
  connector-to-connector which requires manual `relatedConnectorSlugs`). ~1,750 words (in range with the
  4 existing posts, 1,681–2,333 words each). Did **not** add to `public/llms.txt` — checked precedent:
  llms.txt lists page *types* (Blog hub, connector pages, compare pages) not individual posts; none of
  the 4 existing posts are listed there either, so adding this one alone would be inconsistent. / WHY
  NOT the other 2 planned Week-6 posts ("GA4 in Claude: 3-minute setup," "Supermetrics is a pipeline
  not an answer"): hard rule is quality over volume, 1–2 changes per session; those largely overlap
  ground already covered by the existing `how-to-install-arlo-vs-windsor-ai.md` (pipeline-vs-live
  framing) and the GA4 connector page's own setup steps — worth a fresh angle in a dedicated session
  rather than rushing a redundant post here. / VERIFY: `npm install && npm run build` — compiled
  successfully; confirmed via the regenerated `src/content/blog/manifest.json` (build artifact, not
  staged) that the new post parses correctly and is picked up by `getAllBlogPosts()` (slug, date,
  modifiedDate all correct); same pre-existing `(app)/*` + `/welcome` Convex-client prerender failures
  as every prior session (missing `NEXT_PUBLIC_CONVEX_URL` in this sandbox), confirmed by comparing the
  failing-route list to the pre-diff baseline (identical 9 routes: admin, clients, connections,
  dashboard, settings/billing, settings/mcp, solo-dashboard, team, welcome). `npm run lint` clean (only
  the same pre-existing unrelated `sitemap/page.tsx` CSS-tag warning). `git status` shows only the new
  markdown file — reverted the build-regenerated `manifest.json` diff before staging (same pattern as
  every prior session; committed manifest.json is intentionally `[]`, rebuilt fresh on every deploy).

NEXT (Week 7, see 12-week-strategy.md):
1. **Get a human answer on catalog.ts** for the 9 connectors in the Jul 18 product commit (open since
   2026-07-20, now 2 sessions old) — blocks Meta Ads MCP and 8 other potential connector-matrix pages.
2. Two more Week-6/7 blog posts with genuinely new angles (not redundant with what's live): a
   GA4-specific how-to ("GA4 in Claude: the 3-minute setup," concrete/screenshot-style walkthrough
   distinct from the existing Windsor comparison and 100-questions posts) and/or a persona-anchored
   piece from `audits/content.md`'s list (e.g. "How agency owners can spot churn risk 6 weeks before a
   client leaves" — expands the agency-owner persona page's strongest claim into a full piece).
3. `/compare/whatagraph-vs-arlo` — already live? Confirmed both `whatagraph-vs-arlo` and
   `agencyanalytics-vs-arlo` slugs exist in `comparisons.ts` (shipped in an untracked prior session,
   not previously logged) — the comparison hub is further along than the 12-week-strategy.md Week 7
   plan assumed. Remaining comparison gap per the audit: `/compare/funnel-vs-arlo` (funnel.io, KD 17,
   170/mo brand term) is the one named competitor still without a page.
4. Still open (deferred 7x now): static-render the marketing pages (move
   `ConvexAuthNextjsServerProvider` into an app-only route group). Test auth end-to-end after.
5. Submit sitemap + all new pages in GSC (needs a human with Search Console access — unchanged). No SE
   Ranking rank-tracking project for askarlo.app yet either (flagged since 2026-07-10) — worth a human
   wiring one up so future sessions can check real ranking movement instead of keyword-difficulty
   snapshots.

## 2026-07-20 (Mon) — Week 5/6: `/compare` index page + flag catalog.ts/registry drift

IMPORTANT FINDING (read before picking NEXT item 1): since the last SEO session, 5 product
commits landed (`6a4f8f4` "9 new/real connectors: Meta (ads+organic), HubSpot, Salesforce,
Mailchimp, MailerLite, Pipedrive, GoHighLevel, Clarity, GTM", dated Jul 18) that rewrote
`coming_soon`/`waitlist` stub connectors into real working implementations — OAuth
start/callback routes, real API calls, and a new "More platforms" grid on
`/connections`. All 9 are wired into `src/lib/connectors/registry.ts` (functionally live).
**But `src/lib/connectors/catalog.ts` — the file that actually drives public status/marketing
claims — was NOT touched by that commit.** It still says: `meta_ads: "coming_soon"` (eta "May
2026", already past), `hubspot: "coming_soon"` (eta "July 2026" — this month), `salesforce:
"waitlist"`, `mailchimp: "coming_soon"` (eta "August 2026"), `mailerlite: "coming_soon"`,
`pipedrive: "waitlist"`; `gohighlevel`/`clarity`/`gtm` aren't in the catalog at all. This is
the exact "feature-flagged vs. stale catalog entry" ambiguity flagged since 2026-07-06 for
Meta Ads specifically — now six sessions old and now applying to 8 more connectors. Did
**not** ship a "Meta Ads MCP" (or any of the other 8) connector page this session on the
strength of a commit message alone — that would risk overclaiming if catalog.ts is stale on
purpose (e.g. soft-launch/OAuth-testing before a public announcement) rather than by
oversight. **Needs a human call**: either flip `catalog.ts` status to `"live"` for whichever
of these 9 are actually ready for agencies to connect today, or confirm the catalog is
intentionally lagging and say when it'll flip. Once confirmed live, these are the next
connector-matrix pages (Meta Ads MCP alone is a named target keyword in the original audit).

SHIPPED — `/compare` (new hub/index page), closing the gap flagged 3x now (2026-07-04,
2026-07-09, 2026-07-13/16 NEXT lists): every `/compare/[slug]` page's breadcrumb JSON-LD and
on-page breadcrumb link to `/compare`, but no page existed there (soft 404-adjacent gap).
- PAGE: /compare (new) / CHANGE: new `src/app/compare/route.ts` + `_shell.html` +
  `_content/compare.ts`, modeled on the existing `/destinations` hub pattern (the closest
  existing precedent — a catalog-driven card grid — since `/connect` and `/services` have no
  index page either). Card grid pulls all 4 comparison pages live from
  `getAllComparisonSlugs()`/`getComparisonBySlug()` in `src/content/comparisons.ts` (no
  hardcoded duplicate data — new comparison pages auto-appear here with zero further edits).
  Each card shows the real `saasCategory` + the page's own `problem.heading` as the blurb (no
  fabricated copy). Closing CTA is honest about scope: "we only publish a comparison once
  we've researched the competitor's real feature set" + a `/contact` link to request one not
  yet listed, rather than implying full market coverage. / TARGET: internal-linking/crawl-depth
  fix primarily; secondarily "arlo alternatives comparison" / "arlo vs" head-of-funnel intent /
  WHY: explicit repeat NEXT item, low effort relative to impact (fixes a real gap on 4 already-
  indexed pages) / VERIFY: rebuilt clean; directly rendered the compiled shell against the same
  mustache-lite `renderPage()` engine `/compare/[slug]` uses (dynamic route, not statically
  prerendered, so no `.html` output to grep) — confirmed correct `<title>`, canonical
  (`/compare`), H1, all 4 card hrefs (`/compare/windsor-ai-vs-arlo` etc.), CollectionPage +
  BreadcrumbList + ItemList + FAQPage JSON-LD all present, zero unresolved `{{mustache}}`
  tokens, nav/footer components render.
- SITEMAP: added `/compare` to `sitemap.ts` (priority 0.9, weekly — same tier as
  `/destinations`, the closest analog).
- SITE: public/llms.txt — added "Compare ARLO (hub)" to the Pages list.
- SKIPPED (deliberately): any of the 9 newly-real connectors (see finding above — blocked on
  a human catalog.ts decision, not a content-writing task); static-render marketing pages
  (deferred 6x now, still infra/auth-risk, still better as its own focused session); GSC
  submission + SE Ranking rank-tracking project (both need human access, unchanged).
- VERIFY (whole session): `npm install && npm run build` — compiled successfully; only the
  same pre-existing `(app)/*` + `/welcome` prerender failures as every prior session (missing
  NEXT_PUBLIC_CONVEX_URL in this sandbox, Vercel-only secret) — now also includes the new
  `/connections` app route from the Jul 18 connector commit, same missing-env-var cause, not a
  regression from this session's diff. `npm run lint` clean (only the pre-existing unrelated
  `sitemap/page.tsx` CSS-tag warning). `git status` shows only `src/app/sitemap.ts` and
  `public/llms.txt` touched plus the new `src/app/compare/{route.ts,_shell.html,_content/}`
  files — reverted the build-generated `src/content/blog/manifest.json` diff before staging.

NEXT (Week 6, see 12-week-strategy.md):
1. **Get a human answer on catalog.ts** for the 9 connectors in the Jul 18 commit (see
   finding above) — this now blocks more connector-matrix pages than just Meta Ads. Once
   confirmed, ship pages for whichever are flipped to `"live"` (Meta Ads MCP is the
   highest-value one per the original keyword audit).
2. Still open (deferred 6x now): static-render the marketing pages (move
   ConvexAuthNextjsServerProvider into an app-only route group). Test auth end-to-end after.
   Compounds across every marketing page (TTFB/caching) — worth doing before the site adds
   many more marketing pages.
3. Blog engine (Week 6 per 12-week-strategy.md) — first 3 posts from `audits/content.md`
   ("What is an MCP server (for marketers)", "GA4 in Claude: the 3-minute setup",
   "Supermetrics is a pipeline, not an answer"). Nothing left unshipped in the
   connector/comparison clusters that isn't blocked on item 1 or a human.
4. Submit sitemap + new pages (including `/compare`) in GSC (needs a human with Search
   Console access — this routine has no outbound web access). Also still no SE Ranking
   rank-tracking project for askarlo.app (flagged since 2026-07-10) — worth a human wiring
   one up so future sessions can check real ranking movement instead of keyword-difficulty
   snapshots.

## 2026-07-17 (Fri) — Weekly SEO pulse: SE Ranking rank-tracking gap still open (2nd pulse running, no project for askarlo.app; CI/build green, GSC/GA4 still pending) — see reports/outbox/2026-07-17-weekly-pulse.json.

## 2026-07-16 (Thu) — Week 4/5: YouTube MCP connector page

SHIPPED — `/connect/youtube-mcp`, per NEXT item 4 from the 2026-07-13 entry (connector-matrix
work, since Meta Ads status is still unconfirmed) and the 12-week-strategy.md Week 4 plan
("YouTube MCP" completes the Google-source connector matrix — GA4, Google Ads, Search
Console, now YouTube).
- PAGE: /connect/youtube-mcp (new) / CHANGE: new connector page in `src/content/connectors.ts`,
  same `/connect/[slug]` template as the existing three connector pages (no template changes
  needed). Metrics/dimensions sourced from the real live implementation
  (`src/lib/connectors/youtube.ts`, status "ga"): views, watch minutes, average view
  duration/percentage, subscribers gained/lost, likes/dislikes/comments/shares, annotation
  CTR; dimensions day/video/country/deviceType/trafficSourceType — no fabricated features.
  Setup flow matches the real 3-step OAuth pattern (`isConfigured` requires
  `ctx.client.youtubeChannelId`, same Google OAuth grant as GA4/Ads/GSC). FAQ is honest that
  this covers organic channel analytics only, not YouTube ad spend (that's the separate
  Google Ads connector). / TARGET: "youtube mcp" / "youtube analytics mcp" / "connect youtube
  to claude" — checked live SE Ranking data first (`getKeywordsMetrics`, US): all three return
  `is_data_found: false` (no measurable volume yet), same situation "search console mcp" was
  in when it shipped 2026-07-06. Shipping anyway per the README/12-week-strategy rationale:
  seeding ahead of the MCP-adoption growth curve, completes the exact-fit 6-source connector
  matrix competitor.md flags as a defensible, product-true set ("connect shopify/youtube/gsc
  to claude" — ★★★★ product-fit, emerging/fragmented SERP) / WHY: next open connector-matrix
  item now that comparison-page cluster is done and Meta Ads is still blocked on human
  confirmation of catalog status / VERIFY: rebuilt + confirmed static output at
  `.next/server/app/connect/youtube-mcp.html` — correct `<title>` ("YouTube MCP | Connect
  YouTube Analytics to Claude — ARLO"), correct canonical (`/connect/youtube-mcp`), FAQPage +
  HowTo JSON-LD both present; confirmed present in sitemap output (connectorSlugs is derived
  from `getAllConnectorSlugs()`, no sitemap.ts change needed).
- INTERNAL LINKS (orphan-page risk): added `"youtube-mcp"` to `relatedConnectorSlugs` of both
  `google-analytics-mcp` and `search-console-mcp` (already-indexed); new page's own
  `relatedConnectorSlugs` set to those two siblings, `relatedServiceSlugs: ["agency-owner",
  "account-manager"]` (agency-owner's own copy already lists "GA4, Search Console, Google
  Ads, YouTube, and Business Profile" as the OAuth-grant scope — direct fit).
- SITE: public/llms.txt — added the new connector page to the "Pages" list (same pattern as
  every prior connector/comparison page).
- VERIFY (whole session): `npm install && npm run build` — same pre-existing `(app)/*` +
  `/onboarding` + `/welcome` prerender failures as every prior session (missing
  NEXT_PUBLIC_CONVEX_URL in this sandbox, Vercel-only secret) — confirmed byte-for-byte
  identical failing-route list via `git stash` + clean rebuild on unmodified `main` vs. after
  this session's diff. `npm run lint` clean (only the pre-existing unrelated
  `sitemap/page.tsx` CSS-tag warning). `git status` shows only `src/content/connectors.ts`
  and `public/llms.txt` touched — reverted the build-generated
  `src/content/blog/manifest.json` diff before staging (regenerated by
  `scripts/generate-blog-manifest.mjs` on every build, not a real content change).
- SKIPPED (deliberately): Meta Ads confirmation (still needs a human — unchanged), the
  `/compare` index page (flagged 2026-07-13 as worth a dedicated session — still true, didn't
  want to split this session's quality bar across two half-done things), static-render
  marketing pages (deferred 5x now — infra/auth-risk change, still better as its own focused
  session per hard rule "never break the live site").

NEXT (Week 5, see 12-week-strategy.md):
1. Confirm real Meta Ads launch status with the product owner (open since 2026-07-06), then
   either ship "Meta Ads MCP" or leave it out until `catalog.ts` says live.
2. A `/compare` index page — needs actual layout design (no existing hub-page pattern to
   clone), fixes the breadcrumb-implies-a-page-that-doesn't-exist gap on all 4 compare pages.
   Flagged 2x now as worth a dedicated session.
3. Still open (deferred 5x now): static-render the marketing pages (move
   ConvexAuthNextjsServerProvider into an app-only route group). Test auth end-to-end after.
4. Connector matrix is now GA4 + Google Ads + Search Console + YouTube (all `live`/`ga`
   catalog entries covered except Meta Ads, blocked on #1). No further live connectors to
   template until Meta Ads or another `coming_soon`/`waitlist` entry (Shopify, LinkedIn Ads,
   TikTok Ads) ships. Next content push should shift to the 12-post blog plan in
   `audits/content.md` (persona + MCP education + comparison-anchor posts) — nothing left
   unshipped in the connector/comparison clusters that isn't blocked on a human.
5. Submit sitemap + new pages in GSC (needs a human with Search Console access — this
   routine has no outbound web access). Also still no SE Ranking rank-tracking project for
   askarlo.app (flagged 2026-07-10) — worth a human wiring one up so future sessions can
   check real ranking movement instead of just keyword-difficulty snapshots.

## 2026-07-13 (Mon) — Week 4: Whatagraph comparison page

SHIPPED — `/compare/whatagraph-vs-arlo`, per NEXT item 2 from the 2026-07-09 entry (next-
softest comparison term after AgencyAnalytics).
- PAGE: /compare/whatagraph-vs-arlo (new) / CHANGE: new comparison page in
  `src/content/comparisons.ts`, same `/compare/[slug]` template as the existing three pages
  (no template changes needed). Whatagraph's real profile per competitor.md §2 is "agency
  reporting/dashboards" — same category as AgencyAnalytics — so this page is deliberately
  differentiated on angle rather than restating it: AgencyAnalytics's page argues "no
  dashboard to configure/brand"; this one argues "no report template + delivery schedule to
  build and babysit," reflecting Whatagraph's actual emphasis on scheduled/automated report
  delivery (email/Slack/PDF) on top of dashboards. FAQ is honest about the tradeoff —
  Whatagraph still wins when a client contractually expects a recurring report in their
  inbox on a fixed cadence; ARLO has no report builder or delivery schedule at all. Did NOT
  fabricate Whatagraph's current pricing (no outbound web access in this sandbox to verify)
  — table describes it qualitatively ("per-workspace/client pricing, scales with client
  count") instead of inventing a dollar figure, same approach as the AgencyAnalytics page.
  / TARGET: "whatagraph alternative" (KD 7, 50/mo per competitor.md §3B) / WHY: tied-softest
  comparison term in the keyword set alongside AgencyAnalytics, explicit NEXT item / VERIFY:
  rebuilt + directly invoked the compiled route handler for slug `whatagraph-vs-arlo` (since
  `/compare/[slug]` is a dynamic route.ts, not statically prerendered) — HTTP 200, FAQPage
  JSON-LD present, correct canonical (`/compare/whatagraph-vs-arlo`), correct unique
  `<title>`; confirmed present in `sitemap.xml` output (comparisonSlugs is already derived
  from `getAllComparisonSlugs()`, no sitemap.ts change needed).
- SITE: public/llms.txt — added the new compare page to the "Pages" list (same pattern as
  every prior comparison/connector page).
- SKIPPED (deliberately, in favor of the above): NEXT items 1 (Meta Ads launch status —
  still needs a human decision, unchanged since 2026-07-06), 3 (static-render marketing
  pages — infra change with real regression risk to auth, better done as its own focused
  session, deferred 4x now), 5 (GSC submission — needs human Search Console access). Did
  NOT build the item-4 `/compare` index page this session: unlike the connector/comparison
  templates, there's no existing index-page pattern anywhere in the app to clone from
  (`/services` and `/connect` are also just `[slug]` + named folders, no hub page), so it
  needs real layout/design work rather than the data-plumbing this routine is scoped for —
  flagging for a dedicated session rather than shipping something rushed.
- VERIFY (whole session): `npm install && npm run build` — same pre-existing `(app)/*` +
  `/onboarding` + `/welcome` prerender failures as every prior session (missing
  NEXT_PUBLIC_CONVEX_URL in this sandbox, Vercel-only secret) — confirmed byte-for-byte
  identical failing-route list on a clean `main` checkout before vs. after this session's
  diff (diffed the two build logs directly). `npm run lint` clean (only the pre-existing
  unrelated `sitemap/page.tsx` CSS-tag warning). `git status` shows only
  `src/content/comparisons.ts` and `public/llms.txt` touched — reverted the build-generated
  `src/content/blog/manifest.json` diff before staging (it's rewritten by
  `scripts/generate-blog-manifest.mjs` on every build, not a real content change).

NEXT (Week 4 → Week 5, see 12-week-strategy.md):
1. Confirm real Meta Ads launch status with the product owner (still open since 2026-07-06),
   then either ship "Meta Ads MCP" or leave it out until `catalog.ts` says live.
2. A `/compare` index page — needs actual layout design (no existing hub-page pattern to
   clone), fixes the breadcrumb-implies-a-page-that-doesn't-exist gap on all 4 compare
   pages. Worth a session of its own rather than rushing it inside a twice-weekly pass.
3. Still open (deferred 4x now): static-render the marketing pages (move
   ConvexAuthNextjsServerProvider into an app-only route group). Test auth end-to-end after.
   Keeps getting deprioritized for new-page wins; worth doing soon — compounds across every
   marketing page (TTFB/caching), not just one.
4. Comparison-page cluster for the low-KD set is now essentially done (Windsor, Supermetrics,
   AgencyAnalytics, Whatagraph all live) — next content push should shift to either (a) the
   remaining connector-matrix pages once Meta Ads status is confirmed, or (b) the 12-post
   blog plan in audits/content.md (persona + MCP education + comparison-anchor posts).
5. Submit sitemap + new pages in GSC (needs a human with Search Console access — this
   routine has no outbound web access). Also worth having a human wire up an SE Ranking
   project for askarlo.app — the 2026-07-10 pulse flagged there's still no rank tracking.

## 2026-07-10 (Fri) — Weekly SEO pulse: rank tracking gap found (no SE Ranking project for askarlo.app; leads/traffic still pending GSC/GA4 wiring) — see reports/outbox/2026-07-10-weekly-pulse.json.

## 2026-07-09 (Thu) — Week 3: AgencyAnalytics comparison page

NOTE ON THIS SESSION: this routine's checkout started before the 2026-07-06 Search Console
MCP session had pushed. Its first pass duplicated the already-shipped connector-matrix work
(GA4/Google Ads MCP pages) almost line-for-line — caught it at push time (`git fetch` showed
origin ahead), discarded the redundant local commit with `git reset --hard origin/main`
before pushing anything, and re-picked the next open item from the *current* worklog instead.
No duplicate content shipped. Flagging in case the trigger cadence is producing overlapping
runs — worth a human look if this recurs.

SHIPPED — `/compare/agencyanalytics-vs-arlo`, per NEXT item 2 from the 2026-07-06 entry.
- PAGE: /compare/agencyanalytics-vs-arlo (new) / CHANGE: new comparison page in
  `src/content/comparisons.ts`, cloned from the existing `/compare/[slug]` template (no
  template changes needed — comparisons.ts + sitemap.ts already handle new slugs generically).
  Positioning deliberately differs from the Supermetrics/Windsor pages (those argue "no
  warehouse"; AgencyAnalytics isn't a warehouse/ETL tool, it's a white-label dashboard
  builder) — argues "no dashboard to configure/brand/maintain, ask Claude instead" per
  competitor.md's actual profile of AgencyAnalytics ("agency dashboards + white-label client
  reporting"). FAQ is honest about the tradeoff: AgencyAnalytics still wins when a client
  needs a self-serve branded login or a recurring PDF deliverable — ARLO doesn't do
  dashboards at all. Did NOT fabricate AgencyAnalytics' current pricing (couldn't verify it —
  no outbound web access in this sandbox); the feature table describes it qualitatively
  ("per-client/seat pricing, scales with client count") instead of inventing a dollar figure.
  / TARGET: "agencyanalytics alternative" (KD 7, 480/mo per competitor.md §3B, $9.54 CPC) /
  WHY: softest-KD comparison term in the whole keyword set, explicit NEXT item / VERIFY:
  builds static (.html/.rsc/.meta present), FAQPage JSON-LD present in output HTML, own
  canonical + unique <title>, appears in sitemap.xml automatically (comparisonSlugs is
  already derived from `getAllComparisonSlugs()`).
- SITE: public/llms.txt — added a "Pages" entries for all 3 existing connector pages and all
  3 compare pages (Supermetrics, Windsor, AgencyAnalytics) — none of the prior sessions had
  added these despite shipping them; llms.txt is the one file explicitly meant to guide AI
  crawlers, so it should list every indexable page.
- VERIFY (whole session): `npm install && npm run build` — same pre-existing `(app)/*` +
  `/onboarding` + `/welcome` + `/demo/settings/billing` prerender failures as every prior
  session (missing NEXT_PUBLIC_CONVEX_URL in this sandbox, Vercel-only secret) — confirmed
  not a regression, identical failing-route list before/after this session's diff. `git
  status` shows only `src/content/comparisons.ts` and `public/llms.txt` touched.

NEXT (Week 3 → Week 4, see 12-week-strategy.md):
1. Confirm real Meta Ads launch status with the product owner (still open from 2026-07-06),
   then either ship "Meta Ads MCP" or leave it out until `catalog.ts` says live.
2. `/compare/whatagraph-vs-arlo` (KD 7, similar profile/positioning to AgencyAnalytics —
   white-label dashboards, not a warehouse) — next softest comparison term.
3. Still open (deferred 3x now): static-render the marketing pages (move
   ConvexAuthNextjsServerProvider into an app-only route group). Test auth end-to-end after.
   Keeps getting deprioritized in favor of new-page wins; worth doing soon since it compounds
   (TTFB/caching benefits every page, not just one).
4. A `/compare` index page — the breadcrumb pattern on every compare page implies one exists;
   it doesn't yet. Low effort, fixes an internal 404-adjacent gap.
5. Submit sitemap + new pages in GSC (needs a human with Search Console access — this routine
   has no outbound web access).

## 2026-07-06 — Week 2 cont'd: Search Console MCP connector page

SHIPPED — third page in the connector-page matrix (see keyword-research.md CLUSTER 1,
worklog NEXT item 1 from 2026-07-04). Skipped "Meta Ads MCP" from that NEXT item this
session: `src/lib/connectors/catalog.ts` still marks `meta_ads` status `"coming_soon"`
(ETA "May 2026", already past) even though `src/lib/connectors/meta-ads.ts` has a real
implementation wired into the registry — ambiguous enough (feature-flagged vs. just a
stale catalog entry) that shipping a page claiming live Meta Ads support risked
overclaiming, which the prior NEXT note explicitly warned against. Flagging for a human
to confirm actual Meta Ads launch status before that page gets built.

- PAGE: /connect/search-console-mcp (new) / CHANGE: new connector page from the
  `src/content/connectors.ts` + `src/app/connect/[slug]/page.tsx` template, sourced from
  the real `src/lib/connectors/gsc.ts` implementation (metrics: clicks, impressions, ctr,
  position; dimensions: query, page, country, device, date, searchAppearance — no
  fabricated features) / TARGET: "search console mcp" / "google search console mcp"
  (no measurable US volume yet per keyword-research.md, but named explicitly in the
  README's CLUSTER-1 wedge list — seeding ahead of the MCP-adoption growth curve, same
  rationale as the GA4/Google Ads pages) / WHY: completes the 3-page Google-OAuth trio
  (GA4, Google Ads, Search Console) so the wedge covers every `provider: "google"` live
  connector / VERIFY: builds, FAQPage+HowTo JSON-LD present, own canonical
  (`/connect/search-console-mcp`), unique title.
- INTERNAL LINKS (orphan-page risk): added `"search-console-mcp"` to the
  `relatedConnectorSlugs` of both `google-analytics-mcp` and `google-ads-mcp` (already-
  indexed, already-linked-from-service-pages), and set the new page's own
  `relatedConnectorSlugs` back to both siblings + `relatedServiceSlugs: ["seo-specialist"]`.
  Confirmed present in built HTML both directions (GA4/Google Ads pages link to the new
  page; new page links to /services/seo-specialist).
- VERIFY (whole session): `npm install && npm run build` — compiles clean; only the
  pre-existing `(app)/*` authenticated-route prerender failures (missing Convex env vars
  locally, confirmed as pre-existing in the prior session's log too) — not a regression.
  `npm run lint` clean. `git status` shows only `src/content/connectors.ts` touched.

NEXT (continue Week 2 → Week 3, see 12-week-strategy.md):
1. Confirm real Meta Ads launch status with the product owner, then either ship
   "Meta Ads MCP" (if actually live) or leave it out of the connector matrix until it is.
2. Build `/compare/agencyanalytics-vs-arlo` (KD 7) from the compare template —
   still the next comparison-hub page per the 2026-07-04 NEXT list.
3. Still open (deferred twice now): static-render the marketing pages (move
   ConvexAuthNextjsServerProvider into an app-only route group so `/`, `/services/*`,
   `/compare/*`, `/connect/*`, `/work/*`, `/blog/*`, `/about`, `/contact` stop returning
   Cache-Control:no-store). Test auth end-to-end after — do not break login/MCP OAuth.
4. Submit the new /connect pages + sitemap in GSC; request indexing (needs a human with
   GSC access — this sandbox has no outbound web access).

## 2026-07-04 — Week 2: connector-page template + GA4/Google Ads MCP pages

CONTEXT: Week 1 foundation items (stable sitemap lastmod, default OG image, automation
scaffolding) were already shipped in the prior session's later commits (dc38f4f, f4e2997)
but never logged here — noting for the record. Static-rendering the marketing pages
(moving ConvexAuthNextjsServerProvider out of root layout) is still open; deferred this
session in favor of the higher-priority Week 2 wedge per operator FOCUS instructions.

SHIPPED — connector-page matrix (the MCP-integration wedge, see keyword-research.md CLUSTER 1)
- NEW: `/connect/[slug]` data-driven template (src/app/connect/[slug]/page.tsx +
  src/content/connectors.ts), modeled on the existing `/compare/[slug]` pattern. Each page:
  TL;DR, "why connect" section, real 3-step OAuth+MCP-URL setup (matches actual onboarding
  flow in src/content/shared.ts), 5 example prompts, supported metrics/dimensions pulled
  from the actual live connector implementations (src/lib/connectors/ga4.ts,
  google-ads.ts) — no fabricated features — FAQPage + HowTo JSON-LD, internal links to
  matching /services/* persona + /compare/supermetrics-vs-arlo + the sibling connector page.
- PAGE: /connect/google-analytics-mcp / CHANGE: new page / TARGET: "google analytics mcp"
  (390/mo, KD 21, +39x YoY, lowest competition in the cluster) + "connect ga4 to claude" /
  VERIFY: builds, FAQPage+HowTo schema present, canonical set.
- PAGE: /connect/google-ads-mcp / CHANGE: new page / TARGET: "google ads mcp" (590/mo,
  KD 18, +8x YoY) / VERIFY: builds, schema present, canonical set.
- INTERNAL LINKS (fixes orphan-page risk): added optional `relatedConnectorSlug` to
  ServicePillar; /services/seo-specialist and /services/account-manager now link to
  /connect/google-analytics-mcp, /services/google-ads-specialist links to
  /connect/google-ads-mcp. Confirmed present in built HTML for all three.
- SITEMAP: added connector slugs to sitemap.ts (same STATIC_LASTMOD pattern as compare/services).
- VERIFY (whole session): `npm install && npm run build` — compiles + type-checks clean;
  the only failing prerender paths are the pre-existing (app)/* authenticated routes that
  fail locally on missing Convex env vars (confirmed identical failure on a clean checkout
  of main before any changes in this session — not a regression). `npm run lint` clean.
  Confirmed via built .html output: both /connect pages render FAQPage+HowTo JSON-LD and
  correct <title>; cross-links resolve both directions (service pages -> connector pages
  -> services/compare/sibling connector).

NEXT (continue Week 2 → Week 3, see 12-week-strategy.md):
1. Ship "Search Console MCP" + "Meta Ads MCP" connector pages using the same
   src/content/connectors.ts template (GSC is `live`; Meta Ads is `coming_soon` per
   src/lib/connectors/catalog.ts — either wait for GA or clearly label as upcoming, don't
   overclaim live support).
2. Build `/compare/agencyanalytics-vs-arlo` (KD 7) from the compare template.
3. Still open from Week 1: static-render the marketing pages (move
   ConvexAuthNextjsServerProvider into an app-only route group so `/`, `/services/*`,
   `/compare/*`, `/connect/*`, `/work/*`, `/blog/*`, `/about`, `/contact` stop returning
   Cache-Control:no-store). Test auth end-to-end after — do not break login/MCP OAuth.
4. Submit the new /connect pages + sitemap in GSC; request indexing.

## 2026-07-04 (Sat) — Phase 1 audit + P0 + P1 foundation

AUDIT
- Ran full multi-specialist audit (technical, content, schema, geo, sxo, keyword, competitor,
  backlinks) → docs/seo-strategy/audits/* + README.md (health ~47/100). Thesis: win the low-KD
  MCP-integration cluster via a connector-page matrix + comparison hub; don't fight DA-88 incumbents.

P0 — CRITICAL (all shipped, build-gated)
- PAGE: root layout + homepage / CHANGE: removed hardcoded site-wide canonical (was inherited by ~17
  pages as homepage duplicates), added metadataBase + per-page canonical / TARGET: indexation /
  WHY: ~17 inner pages self-reported as homepage dupes / VERIFY: inner pages now emit own canonical.
- PAGE: homepage / CHANGE: removed fabricated testimonials (fake names marked "placeholder"); section
  auto-hides / WHY: trust/compliance / VERIFY: no testimonials render.
- PAGE: public/llms.txt / CHANGE: corrected pricing tiers + removed dead /pricing,/docs links /
  TARGET: AI citation / VERIFY: llms.txt accurate.
- PAGE: /services/meta-ads-specialist / CHANGE: launched (tier-2 config was complete but slug route
  404'd) / VERIFY: page 200 + in static build.
- PAGE: homepage / CHANGE: replaced dead+wrong JSON-LD with correct ARLO Organization +
  SoftwareApplication+Offers + Person + WebSite + BreadcrumbList; wired into homepage /
  TARGET: entity clarity / VERIFY: view-source shows SoftwareApplication.
- PAGE: /about, /services, /work, blog author / CHANGE: purged all FuturLabs/Choquer custom-software
  copy; rewrote for standalone ARLO; founder = Bryce Choquer, Founder & CEO / TARGET: entity identity /
  VERIFY: grep for futurlabs/custom-software/mid-market = clean.
  (KEPT the homepage TrustBar — those 40+ logos are real ARLO beta users, confirmed by owner.)

P1 — HIGH (shipped this session)
- PAGE: /compare/supermetrics-vs-arlo / CHANGE: new comparison page (TCO + FAQPage) from windsor
  template / TARGET: "supermetrics alternative" (KD 16, $33.76 CPC) / VERIFY: page builds + FAQ schema.
- PAGE: FAQ component / CHANGE: emit FAQPage JSON-LD (propagates to homepage + all service pages);
  removed duplicate FAQPage from homepage generateSchema / TARGET: AI Overviews / VERIFY: FAQ schema
  on service pages.
- PAGE: /contact / CHANGE: real metadata via contact/layout.tsx (was inheriting homepage) / VERIFY:
  unique title/desc.
- PAGE: app/auth/demo/token routes / CHANGE: noindex via robots.txt disallow + X-Robots-Tag header /
  WHY: keep app routes out of the index / VERIFY: X-Robots-Tag: noindex on /sign-in etc.
- SITE: added GA4 gtag (G-CP22NBPLFP) — analytics baseline now collecting.

NEXT (Week 1 — finish foundation; see 12-week-strategy.md):
1. Static-render marketing pages — move ConvexAuthNextjsServerProvider into an app-only route group so
   /, /services/*, /compare/*, /work/*, /blog/*, /about, /contact stop returning Cache-Control:no-store.
   TEST AUTH END-TO-END after (do not break the app login/MCP OAuth).
2. Add a default 1200×630 OG image in public/ + reference in root metadata (0 pages have one today).
3. Fix sitemap.ts lastModified (all URLs currently stamped "now"); submit sitemap in GSC + request
   indexing for homepage + top 5 pages.
4. Then Week 2: build the /connect/[slug] connector-page template + ship "Connect GA4 to Claude" and
   "Google Ads MCP" (the wedge).
